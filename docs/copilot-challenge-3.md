# 画面コパイロット 第3次挑戦 — 設計空間の全列挙と判断

作成: 2026-07-15 ／ 最終更新: 2026-07-17 ／ ステータス: **挑戦3 実装中**
本書は Navigator/Copilot（視覚コパイロット機能）の**新しい正本**。前身の
navigator-copilot-plan.md（v3設計）と navigator-stabilization-followups.md（v4精度計画）は
本書に総括を引き継いで old/ へ移す。

> 🔴 **本書の前提となる覚悟（オーナー方針）**: この挑戦で完成する必要はない。
> 第3次も失敗作を作る覚悟でやる。10回目か20回目で成功すればよい。
> ただし、**同じ理由では二度死なない**。そのために挑戦ごとに仮説・合格条件・剖検を
> 記録する（§6 反復ロードマップ）。

---

## 1. 何を作るのか（機能の定義）

**画面を理解して、人間に次の行動を案内する視覚コパイロット。** 自律操作エージェントではない。
最終操作は常に人間が行う。Universal I//O の中の1機能であり、送信レビュー・受信変換と
同じ「中間レイヤー」思想の視覚版。

成功の姿（ユーザー体験としての北極星、v3計画から引き継ぎ）:
> 「開始」を押したら、あとは画面の光る場所を順番に押していくだけで、目的の答えにたどり着く。
> 最後のステップでは案内ではなく目的の答えを読み上げて完了する。

---

## 2. 過去2回の挑戦の総括（事実と死因）

### 第1次: プロトタイプ〜v3（自由文マーカー方式、2026-07-05〜07-13）

- **構成**: 強いマルチモーダルモデル1コール＋自由文回答＋`[[target:ラベル]]`マーカー。
  位置解決はOCR/AXのラベル文字列一致。初期はDOMも参照していた。
- **良かった点**: 着想から約15分で動き、体感8割の品質。画面理解そのものは最初から成立していた。
- **死因（実測・記録済み）**:
  1. **同名ラベルの取り違え** — 「概要」が2つある画面でラベル一致は構造的に区別不能。
  2. **再現性のなさ** — マーカーが出たり出なかったり（プロンプト注入の履歴汚染、
     モデルの保守化ドリフト。7/6 の記録）。
  3. **入力の劣化蓄積** — 画像縮小(長辺1,600px)・出力上限(1,200tok)・履歴削除・
     高速初手モデル(qwen)の混入が段階的に積まれ、後期のv3は初期の15分版より
     貧しい入力で動く別物になっていた。
  4. **DOMの喪失** — 「拡張に依存しない」という製品戦略判断でDOMを外し、ブラウザUIで
     最強のセンサーを失った。その穴を後段の複雑な判定で埋めようとした。

### 第2次: v4（4役職分割＋決定論ゲート、2026-07-14〜15）

- **構成**: Observation / 版付きPack / Gateway発行Run（署名・revision）/ Planner /
  Grounder / Verifier / Renderer の役割分離。fail-closedゲート。v3と並走（shadow）。
- **良かった点**: trace/usage/eval の計測基盤。候補収集（AX/OCR、親文脈付き）。
  画面安定検知。レシピ無しの汎用モデル判定が実画面で verified を返せることの実証。
- **死因（実測・記録済み）**:
  1. **配線が壊れていても気づけない** — snapshot競合でv4が一度も動かないまま
     「production readiness」を宣言。ローカルテスト合格をe2e完成と誤認。
  2. **独立でない検証は検証にならない** — 各役職が同じ痩せた入力を見ており、
     多段化は堅牢性でなく同じ誤りの増幅だった。
  3. **正解の決定権の分散** — モデルが正しく理解しても、ラベル一致が別要素を選び、
     ゲートが正しい進捗を拒否した（grounder disagreement → 正答のハイライト抑止を
     ログで確認）。
  4. **フォールバックが失敗を隠す** — 旧経路・別モデルへのサイレント切替で、
     ユーザーが試しているのが新旧どちらかすら不明になった。
  5. **検証順序の逆転** — 最高品質モデルで上限を測る前に、制御構造と速度最適化を
     先に作った。オーナーは一貫して「まず最高品質」と要求していたが、
     実装の受け入れ条件にならなかった。

### 2回に共通するプロセスの死因

- 「動き続けること」を暗黙の制約にした結果、並走・フォールバック・フラグが増殖し、
  二重実装が恒久化した（ビッグバン失敗の反動での過剰防衛）。
- 完了の定義が「コードが書けた」であり、「実画面でその経路が動いた証拠」ではなかった。

---

## 3. 動かせない制約（すべての案が従う前提）

1. **観測の限界**: 見た目が同一で内部状態が異なる画面は、構造情報なしには原理的に判別
   不能。100%はどのアーキテクチャでも約束できない。目標は「観測可能なものを高精度に、
   確証がなければ誤案内せず明示的に止まる」。
2. **製品戦略**: 標準体験はゼロインテグレーション（インストールして権限を許可すれば
   どのアプリでも動く）。拡張・API連携は精度アドオン（個社/上位プラン）として許容。
3. **軽快さと堅牢さが最大プライオリティ**（全体方針）。ただし堅牢さの置き場所は
   決定論的外殻であり、LLMの多段呼び出しではない（第2次の教訓）。
4. **人間が最終操作**。誤ハイライトは人間のミスを誘発するため、エージェント制御と
   同水準の証拠管理が必要。

---

## 4. 設計空間の全列挙

結論に飛びつかないため、各軸の選択肢を利点・欠点・証拠付きで列挙する。
**太字**は§5のCTO判断で採る案。

### 軸A: 認知構造（誰が考えるか）

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| A1 | **単一コール**（理解・案内・対象選択を1回で） | 情報損失なし・接合部なし・最速 | モデル品質に全依存。上限未測定（これを測るのが第3次） |
| A2 | 2段（案内→操作後に進捗判断） | 進捗判断が新しい画面という独立証拠を得る | 「別人格Verifier」ではなく「次ターンの判断」として実装すれば実質A1の反復 |
| A3 | 4役職分割（第2次） | 各段が計測可能・理論上は誤り抑制 | **実測で死亡**。独立でない検証は増幅装置。接合部が壊れる |
| A4 | エージェンティックループ（computer-use型、モデルがツールを自律呼び出し） | 業界の実証多数 | 自律操作向き。人間案内では往復が過剰、体験が重い。将来の自動操作モードでは有力 |

### 軸B: センサー（何を見せるか）

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| B1 | 画像のみ | 依存ゼロ | 座標が出せない・文字精度に限界 |
| B2 | **画像＋OCR** | 文字の正確な位置 | 構造（親子・状態）が無い |
| B3 | **画像＋AX** | 親子関係・selected/expanded・矩形。macOSネイティブに強い | ChromeはAXEnhancedUserInterface依存で品質変動。Electronアプリはまちまち |
| B4 | **画像＋DOM由来情報** | ブラウザUIで最強の構造 | 取得経路が論点: Chrome AX WebArea（拡張不要・DOM由来）で標準経路、拡張は精度アドオン。第1次で外したのは戦略判断であって精度判断ではない |
| B5 | アプリ固有API（GA4 API等） | 最高精度 | 個別連携=ゼロインテグレーション違反。個社ハーネスの領域 |
| B6 | ユーザー協働（見つからない時に人間に探させる/教えさせる） | 原理的限界の脱出口 | UX設計が必要。「確証がなければ止まる」の先の一手として有望、挑戦4以降の候補 |

→ Spot Visionは同一captureの画像とAXを固定する。初期コメントは画像中心、後続質問は同じ画像＋
固定AX候補＋履歴を1コールへ渡す。DOM/OCRはAX候補欠落の実測後に一つずつ加える。

### 軸C: 位置決め（どこを光らせるか）

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| C1 | モデルの自由座標（bbox） | 依存なし | 不正確が実証済み（VLM座標は信頼できない） |
| C2 | ラベル文字列一致（第1次） | 単純 | **同名ラベルで死亡（実測）** |
| C3 | **candidate ID選択**（列挙した候補からモデルがIDで選ぶ） | 座標はコードが保証・同名は親文脈で区別・幻覚した場所を指せない | 候補収集の網羅性に依存。Canvas等で候補ゼロ |
| C4 | Set-of-Marks（画像に番号を焼き込んでモデルに番号で答えさせる） | 研究で実績。候補とビジュアルの対応が確実 | 画像加工の実装。C3で不足した場合の強化オプション（挑戦4候補） |
| C5 | 位置なし案内（「左メニューの下から2番目」と言葉だけ） | 実装ゼロ | 製品価値の核（光る場所を押すだけ）を放棄 |
| C6 | 低信頼bbox経路（候補ゼロのCanvas等のみC1を明示的に使う） | 空白を埋める | 必ず低信頼と表示・記録して使う |

### 軸D: 状態と計画（何を覚えるか）

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| D1 | 全手順を最初に固定（第2次のRun/レシピ） | 監査可能・進捗が明確 | UI変更・想定外画面で脆い。plan照合の配線が壊れて死亡 |
| D2 | **目的だけ保持し、毎ターン「次の一手」を再計画** | 想定外に強い・状態最小 | 迷走の可能性（目的+直前の案内を毎回渡して抑制） |
| D3 | ステートレス（毎回ゼロから） | 最単純 | 目的を見失う。マルチターンの案内が成立しない |
| D4 | サーバ保持の署名付きRun（第2次） | 改ざん耐性・課金/監査 | 検証段階には過剰。**計測資産としてのみ維持**、体験に関与させない |

### 軸E: 進捗検証（進んだと誰が言うか）

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| E1 | 検証なし（モデルの自己申告 `[[step:done]]`） | 単純 | 第1次で誤advance発生 |
| E2 | **決定論の整合性検査＋次ターンのモデル判断** | クリック後の新規captureという独立証拠で判断。検閲ではなく証拠 | 判断自体はモデル品質依存（=第3次で測る対象） |
| E3 | 独立Verifierロール（第2次） | 理論上の二重チェック | 同じ入力を見る限り独立でない。実測で正答を拒否 |
| E4 | typed postcondition（レシピ）による機械判定 | 決定論・高速・安価 | レシピがある経路のみ。**後から載せる精度レイヤー**（§0.7決定を維持） |

### 軸F: モデル戦略

| 案 | 内容 | 利点 | 欠点・証拠 |
|---|---|---|---|
| F1 | **最高品質単一モデル（GPT-5.6 Sol / Responses API / 十分な画像と予算）で上限測定** | 「成立するか」に最初に答えが出る。オーナーの一貫した要求 | 遅い・高い（検証段階は許容と決定済み） |
| F2 | role別の軽量モデル | 安い・速い | 上限測定前にやって死亡（第2次）。**成立確認後の最適化として再登場** |
| F3 | challenger比較（Gemini 3.1 Pro / Claude Fable 5） | 空間理解の差を測れる | F1の後、同一golden setで |
| F4 | ローカルモデル | プライバシー・コスト | 現状の視覚品質では論外。遠い将来 |

### 軸G: 知識（何を知っているか）

| 案 | 内容 | 判断 |
|---|---|---|
| G1 | **純汎用**（何も知らずに画面だけ見る） | 第3次の基礎。これが立たないと製品コンセプトが立たない |
| G2 | 製品レシピ/Pack（GA4の定番手順・完了条件） | 精度レイヤーとして後乗せ（§0.7決定を維持） |
| G3 | RAG/ヘルプ文書参照 | 挑戦5以降の候補 |
| G4 | 個社ハーネス（tenant overlay） | 商材。汎用成立後 |
| G5 | fine-tune | 評価済みtraceが貯まってから |

### 軸H: 誤案内時の安全弁（ゲートの代わりに何を置くか）

| 案 | 内容 | 判断 |
|---|---|---|
| H1 | 信頼度ゲートで表示抑止（第2次） | **廃止**。正答を検閲した実績 |
| H2 | **モデル自身の明示的停止**（`clarification`モードで「確証がないので教えてください」と言わせる） | 採用。誤案内せず止まる責任を認知コアに戻す |
| H3 | **データ整合性検査**（stale capture・存在しないID・範囲外座標の拒否） | 採用・強化。これは検閲ではなく証拠の検査 |
| H4 | ユーザーの「違う」ボタン（誤ハイライトの即時フィードバック） | 挑戦4以降。学習データにもなる |

---

## 5. CTO判断（第3次挑戦の構成）

**選択（2026-07-16更新）: 固定snapshot単一経路 × C3 × D2 × E2 × G1 × H2+H3**

```
Spot Vision（1枚の固定capture）
  ├─ 画像 → Luna VLM → 短い初期コメント
  └─ AX収集を並列実行し、同一captureへ固定
       ↓
後続質問: 同じ画像 + 固定AX候補 + 履歴 → Luna VLM 1コール
  出力: answer|guide|clarification + optional candidate ID
       ↓ candidate IDがある場合のみ
決定論的な矩形変換 → スクリーンショット拡大・赤枠

Copilot（後続段階）
  人間のクリック → 画面安定化 → 新しい固定snapshot → 同じ1ターンを反復
```

**この構成を選ぶ理由（他を選ばない理由）**:
- 第1次の死因（同名ラベル・再現性）はC3とE2が直接潰す。第2次の死因（接合部・検閲・
  入力劣化）はA1とH1廃止が直接潰す。過去の死因に同じ形で当たらない最小構成がこれ。
- ユーザーに内部経路の選択を要求しない。回答と案内を同じ出力契約にし、candidate IDの有無だけで
  決定論的レンダラーを動かす。
- Spot Visionでは画像・AXを途中更新せず、画像だけ古くAXだけ新しい不整合を作らない。
- DOM/OCRは、AX候補欠落の実測後に追加する精度レイヤーとする。
- 堅牢性は決定論的外殻（H3＋trace/replay＋実効モデル記録）で第2次より**強く**なる。

**ただし、これはアイデアであり、実測で棄却されうる。** 確定仕様ではない。

### 検証モードの受け入れ条件（fail-loud）

1. 各比較区間で固定した実効モデル・reasoning effortを開発UIとtraceで確認する。
   使えなければ明示的エラー。フォールバック発生 = テスト失敗。最初の品質上限確認は
   GPT-5.6 Sol / `max`、成立後の最初の軽量化比較はGPT-5.6 Luna / `none`とする。
2. 位置指定は candidate ID のみ（低信頼bbox経路は件数を記録し、UIで低信頼と明示）。
3. 同名ラベル（GA4「概要」×2）を親文脈で正しく区別できる。
4. stale capture / capture scope不一致の採用 0。
5. 実効モデル・経路・fallback有無を毎ターン記録し、開発ビルドで表示。
6. 全ターンが trace から再生可能。
7. 成立確認後に初めて速度・コスト最適化（画像縮小・履歴圧縮・軽量モデル・キャッシュ）に
   着手する。順序の逆転は前回最大の誤り。

---

## 6. 反復ロードマップ（失敗前提）

### 挑戦の運用ルール

- **1挑戦 = 仮説 + 実装 + 実画面計測 + 判定 + 剖検**。挑戦の開始時に仮説と棄却条件を
  本書に書き、終了時に剖検（何が死んだか）を追記する。
- 挑戦の切り替え時は git tag を打つ（例: `challenge-3-start` / `challenge-3-end`）。
- 並走は比較計測の期間のみ（時限）。挑戦N+1 が立った時点で挑戦N の経路は削除する。
- 完了の定義は常に「実画面でその経路が動いた証拠（trace/usage）」。コード完了・
  テスト合格は完了ではない。

### 挑戦3（本書の構成）

開始記録（2026-07-16）:

- 復帰点: tag `challenge-3-start`（main `93f35f7`）。
- 開発branch: `experiment/copilot-challenge-3`。runtime feature flagで新旧を切り替えない。
- 隔離境界: 新Gateway endpoint `/api/ai/screen-understanding` と新しいmacOS Session/Viewを
  白紙実装する。既存`VisionSession`、`GatewayNavigateClient`、`/api/ai/navigate`、v3/v4 engineを
  importしない。共有するのはPanelController、capture、画像表示、入力UI、認証、Gateway transport。
- 最初のMVPは**Vision-first**: immutableな1枚のcaptureについて、短い初期理解を返し、
  追加質問へ答えるところまで。candidate選択、ハイライト、クリック後進捗判定は、このMVPの
  実画面成立を確認するまで接続しない。
- 完了済み: fail-loudなGPT-5.6 Sol Responses API engine/endpointを独立実装。固定条件は
  `gpt-5.6-sol` / `detail: original` / `reasoning.effort: max` / 25,000 output tokens / fallbackなし。
- 完了済み: macOSに`Challenge3VisionSession` / `Challenge3VisionSessionView` /
  `GatewayScreenUnderstandingClient`を独立実装。`SessionCoordinator`のcapture完了後という1箇所だけを
  新Sessionへ切り替え、旧`VisionSession`、Navigator、Copilotへの実行入口を外した。パネル・capture・
  画像表示・入力UI・認証transportは共有している。
- 検証済み（コード）: web test 68件、対象lint、TypeScript、Next production build、macOS Debug
  `CODE_SIGNING_ALLOWED=NO`が成功。
- 初回実画面スモーク（2026-07-16）: GA4の「ユーザーの環境の概要」をcaptureし、実効モデル
  `gpt-5.6-sol`で画面、プロパティ名、表示中の指標を正しく要約した。続く「どのソーシャルメディア
  からアクセスがあるか」という質問には、現在の画面だけでは判定できないと明示し、確認先を案内した。
  UI表示は16,791 ms、Gatewayでは3回の`POST /api/ai/screen-understanding`がすべて200で、
  17.0秒、18.0秒、17.3秒だった。モデル名と経路が開発表示され、fallbackや旧navigate経路への流入は
  観測されなかった。これを**Vision-firstの初期理解＋追加質問の成立**と判定する。
- 未検証: candidate収集・選択、ハイライト、クリック後の新規captureによる進捗判定。今回の成功は
  挑戦3全体やGA4 golden pathの合格を意味しない。
- 方針更新（2026-07-16）: オーナーがVision-firstパネルの初回成立を確認し、次段階への移行を承認。
  Sol / `max`は品質上限の基準として保持し、実行設定をGPT-5.6 Luna / `none`へ固定変更する。
  fallbackなし、`detail: original`、25,000 output token上限、実効設定の開発表示は維持する。
  同一画面でLuna基準を1回確認してから、candidate収集結果の開発表示へ進む。
- 操作経路開始（2026-07-16）: `読む / 案内`を明示モード化。capture時にAX候補を最大250件・
  1秒で固定し、`案内`では画像を送らない。一意一致は`ax_exact`、それ以外はLuna / `none`の
  text-only `ax_llm`、候補不足は明示停止とした。route・実効モデル・AX件数・candidate IDを
  開発UIへ表示し、選択candidateの矩形をスクリーンショット上で自動拡大・赤枠表示する。
  DOM、画像fallback、実画面上の常駐ハイライト、クリック後進捗判定は未接続。
- 操作経路のコード検証: web test 73件、対象lint、TypeScript、Next production build、macOS Debug
  `CODE_SIGNING_ALLOWED=NO`が成功。実画面のAX candidate recall、`ax_exact / ax_llm`選択、赤枠位置は未検証。
- 二モード実験の棄却（2026-07-16）: `読む / 案内`は内部経路の比較には使えるが、ユーザーに
  システム都合の分類を要求して不便であり、Spot Visionの製品UIとして棄却。action専用の
  `ax_exact / ax_llm / ax_unavailable`分岐も同時に削除した。
- Spot Vision統合（2026-07-16）: 入力欄を再び一つにし、初期コメントでは固定画像を軽く読み、
  AXを同時点でバックグラウンド収集する。後続質問は同じ画像・固定AX候補・履歴をLunaの
  `snapshot_vlm` 1コールへ渡す。`target_candidate_id`が返った場合だけ同一captureの候補へ解決し、
  スクリーンショットを拡大・赤枠表示する。AXには取得時間・探索node数・候補数・打切理由を追加し、
  開発UIとusage metadataへ記録する。DOMとCopilotの再capture反復は未接続。
- Spot Vision統合のコード検証: web test 70件、対象lint、TypeScript、Next production build、
  macOS Debug `CODE_SIGNING_ALLOWED=NO`が成功。固定画像＋AXによる実画面回答・赤枠は未検証。
- Spot Vision実画面スモーク（2026-07-16）: GA4国別画面からデバイス別分析を質問し、
  `snapshot_vlm / gpt-5.6-luna`が4,849 msで正しい案内と`ax:144 / ユーザーの環境の詳細`を返した。
  AXは250候補・574 ms・`candidate_limit`で、candidate選択とスクリーンショット自動拡大は成立。
  赤枠が対象より大きく画像左端へはみ出した原因は、rendererがAX矩形を横30%・縦45%膨張させ、
  画像境界へclampしていなかったこと。表示paddingを左右6 pt・上下4 ptへ固定し、画像内の安全領域へ
  clampするよう修正した。修正後の赤枠サイズは実画面再確認待ち。
- Spot Vision劣化調査（2026-07-16）: 同じGA4系画面で案内文は概ね正しかったが、
  `target_candidate_id = null`となり赤枠が出ず、「候補IDに対応する操作対象がない」という内部説明が
  uncertaintiesとして表示された。Gatewayは`snapshot_vlm`で200を返しておりfallbackではない。
  AX診断は250候補・402 ms・`candidate_limit`で、時間切れではなく候補上限による打切りだった。
  収集対象に`AXStaticText / AXRow / AXCell`を含めていたため、GA4の表が候補枠を消費してサイドバーの
  操作対象を落とし得ること、プロンプトがcandidate不在時に文章案内まで弱めていたことを原因と判定。
- 劣化修正（2026-07-16）: AX候補をButton/Link/Tab/Menu/Input等の操作roleへ限定し、ラベルなしの
  操作親配下にあるStaticTextだけは親roleを継承して採用する。上限を500へ拡張した。また、candidateが
  無くても視覚的に根拠のある文章案内を維持し、message/uncertaintiesへAX・DOM・candidate ID等の
  内部語を出さないよう契約を修正。route/model/AX診断/candidate ID/uncertaintiesと生の契約エラーは
  Debug限定表示とし、Releaseでは回答・赤枠・一般ユーザー向けエラーだけを表示する。さらに内部語が
  user-visible出力へ混入したモデル応答はGatewayで拒否する。web test 71件、対象lint、TypeScript、
  Next production build、macOS Debug/Release buildが成功。実画面再確認待ち。
- candidate上限の扱い（2026-07-16）: 500件は一般画面の99%をカバーすると実証した値ではなく、
  操作roleへの絞り込み後に置く暫定の安全上限である。通常の表セル・行・説明文は件数に含めないため、
  一般的なSaaS、管理画面、入力フォームのSpot Vision検証を先に通し、この構造改善は低優先度で保留する。
  `candidate_limit`到達により画面内の正解操作が欠落する実例が再発した場合、またはgolden path 30件で
  上限到達が複数製品にまたがる場合に再開する。解決時は上限値を増やし続けず、(1) ローカルの固定AX/DOM
  snapshot収集とモデル送信上限を分離、(2) 操作階層・重複・反復表を圧縮、(3) 質問関連候補だけを
  stable ID付きで送信、(4) 正解を501番目・1,500番目に置く回帰テスト、の順で実施する。
- Copilot反復の開始境界（2026-07-16）: Spot Visionの最新応答が`guide`かつ同一captureの
  candidate IDを持つ場合だけ「案内を開始」を表示する。開始時に最新のユーザー質問を目的として固定し、
  中央パネルを既存`AppMode.copilot`の右下帯へ切り替え、candidateの正規化矩形をcapture座標へ
  決定論的に戻して実画面へ永続赤枠を表示する。旧`VisionSession`、Navigator、Run、Verifierは
  呼び出さない。macOS Debug build成功。クリック後の新snapshot反復は次コミットで接続する。
- Copilot新snapshot反復（2026-07-16）: 右下帯の表示中だけglobal mouse-upを監視し、クリック後に
  元captureと同じdisplay/regionを最大8回・350 ms間隔で撮影する。48×48 grayscale差分で画面変化を
  検知し、変化後の連続captureが安定閾値内に入った一枚だけを採用する。新captureではAX候補を取り直し、
  `goal + previous_instruction + 新画像 + 新AX`をLunaの単一`snapshot_vlm`へ渡す。`guide`なら新candidateを
  実画面へ永続表示、`answer`なら完了、`clarification`なら自動反復停止、変化/安定を確認できなければ
  fail-loudで手動「再確認」を残す。旧captureの候補は新画像へ持ち越さない。web test 72件、対象lint、
  TypeScript、Next production build、macOS Debug/Release buildが成功。実画面クリック反復は未検証。
- 外部レビュー照合と計測保全（2026-07-17）: レビュー対象snapshot以降に⑦の右下帯、実画面の永続赤枠、
  クリック後の安定capture反復までコード接続済みであるため、現在地は「④完了・⑤途中」ではなく、
  **⑤の実画面計測と⑦の実画面検証が未完**と訂正する。レビューで判明した計測阻害要因は先に修正する。
  全display captureに対してAXはfrontmost appのfocused window（無ければapplication root）だけを探索する
  制約を、対象app/window・AX root・capture scopeとして診断とusageへ明示した。これにより候補欠落を
  モデル選択ミス、AXツリー品質、収集scope制約へ分類する。ラベル付き操作親の子StaticTextを重複候補化
  していた継承も止め、ラベルなし操作親の代理ラベルに限った。`candidate`、`AX`、`DOM`という単語だけで
  正当な英語回答を拒否するGatewayフィルタは、採用管理、Dynamics AX、開発者ツールで誤爆するため削除。
  user-visibleに内部実装を出さない責務はpromptとRelease UI境界で維持する。
- 未知candidate IDの扱い（2026-07-17）: 検証期はGatewayでターン全体をfail-loudのまま拒否する。
  `null`へ補正して文章だけ採用すると、モデルが出力契約を破った事実を成功ターンに見せるためである。
  「候補が存在しないときも文章案内を維持する」は、モデルが自ら`target_candidate_id: null`を返す場合の
  契約であり、存在しないIDを返した場合とは区別する。製品期に部分成功UXへ変えるかは実測後に判断する。
- replay保全（2026-07-17）: Debugビルドだけ、実際に送信した再エンコード後の画像、base64を画像ファイル
  参照へ置換したrequest JSON、生responseまたはerrorをOS一時ディレクトリ
  `UniversalIO-Challenge3-Replays`へ直近20件保存する。ReleaseとSupabase usageには画像・会話・候補本文を
  保存しない。これで⑤の失敗ターンを同じ入力から剖検できる。3MB超のJPEG化、20ターン上限、Chrome
  cold start時のAX候補変動は現時点で挙動を変えず、media type・エラー・AX診断をreplayとtraceで観測する。
- 次回セッション開始点（2026-07-17）: branchは`experiment/copilot-challenge-3`、実装基準は
  `701f19f`。まず新規実装を増やさず、署名付きDebugアプリで次を順に実画面確認する。
  (1) Spot Visionで案内文が維持され、スクリーンショット内の赤枠が対象矩形へ収まる、
  (2) 「案内を開始」後に右下帯と実画面の永続赤枠が出る、(3) 対象をクリックすると新しい安定captureと
  AX候補へ更新され、同じ指示を繰り返さず次の一手または目的の答えを返す。失敗時は開発UIの
  target app/window、`collection_root`、`capture_scope`、打切理由を記録し、Consoleに出る
  `UniversalIO-Challenge3-Replays`のrequest/image/responseを剖検する。⑤と⑦の実画面証拠が揃うまで
  ⑥のDOM等を追加せず、原因をcandidate選択、AX欠落、scope制約、進捗判断へ分類する。
- ローカル起動の署名ルール（2026-07-17）: `CODE_SIGNING_ALLOWED=NO`はコンパイル検証専用で、生成物を
  実画面テストに起動しない。未署名またはad-hoc署名の`/tmp`ビルドは、システム設定で許可済みの
  Apple Development署名版とmacOS TCC上で別主体になり、3権限が未許可に見える。実画面テストは通常の
  Xcode DerivedDataへApple Development署名でDebug buildし、その同じappを起動する。許可済みトグルを
  毎回OFF/ONし直す運用にはしない。画面収録を初回許可した場合だけ、macOS要件に従って再起動する。
- コパイロット運用弁の修正（2026-07-17、第2次外部レビュー反映）: ⑦の実画面検証で確実に踏む欠陥を
  検証開始前に修正した。(1) 手動「再確認」は変化検知ゲートを通さず安定確認のみで新captureを採用する
  — 48×48全画面平均差分の変化閾値1.5%はドロップダウン等の小さなUI変化を原理的に検知できず、
  手動経路まで同じゲートを通すとfail-loudの脱出口が存在しなかった。クリック起点の自動確認は従来どおり
  変化検知を要求する。(2) 安定化ループのattemptごとの差分値・フェーズ・最終判定をDebugビルドの
  Consoleへ記録する（`[Challenge3] stable-capture`）。変化/安定の失敗はGateway呼び出しが発生せず
  replayに残らないため、閾値調整の実測材料はこのログが正本。(3) 進捗guideに上限15ステップの
  決定論的停止弁を追加（`stepLimit`、terminal）。モデル申告以外の停止条件がなく、迷走時はユーザーが
  終了を押すまで課金反復が続いた。迷走の品質改善（完了済みステップ注入等）は挑戦4バックログのまま。
  (4) guideがtarget無しで返った場合、帯の文言を「赤い枠をクリック」から「案内に従って操作」へ分岐
  （存在しない赤枠への誘導を排除。AX候補欠落計測で必ず発生する状態）。(5) コパイロット開始/終了は
  モード遷移の成否を確認し、開始失敗時は状態を巻き戻し、終了失敗時はパネルを閉じる（帯UIが中央
  パネルへ残留する宙ぶらりん防止）。20ターン上限・無関係クリックでの一時消灯は据え置き（観測対象）。
- Chrome web AX消失の剖検と修正（2026-07-17、⑤の最初の実画面計測）: GA4で「赤枠と案内開始が
  一切出なくなった」事象を計測した。replayの実証拠: AX候補66件はすべてChromeのブラウザUI
  （ツールバー・ブックマーク・タブ）で、GA4ページ内要素は0件。visited=99・39ms・complete、
  focused_window収集 — つまりモデルやプロンプトの劣化ではなく、**ChromeがWebコンテンツのAXツリー
  自体を公開していなかった**。7/16に250〜500件取れていたのは当時Chromeのaccessibilityが外部要因で
  有効だったため（Chromiumは近年、無活動でrenderer accessibilityを自動無効化する）。実機実験
  （Chrome 150）: AXManualAccessibilityはChrome本体では-25205（unsupported、Electron専用）。
  AXEnhancedUserInterfaceの設定＋**AXクエリの継続**で約1秒後にAXWebAreaが出現し、以後パスごとに
  ツリーが成長する（99→273→921ノード、揺らぎあり）。単発1秒収集では原理的に間に合わない。
  なおAXEnhancedUserInterface属性の有無はブラウザ判定に使えない（Finderにも列挙される）。
  修正: 収集開始時にAXManualAccessibilityとAXEnhancedUserInterfaceの両方を設定し、収集を
  複数パス化（最大5パス・全体5秒・パス間500ms。初回パスでWebArea不在なら再試行、WebAreaありで
  ノード数が25%以上成長中なら再試行、最多候補のパスを採用）。診断へ`collection_passes`と
  `web_area_present`を追加しGateway検証・Debug UI・Consoleログ（`[Challenge3] ax-collect`）へ
  記録。ネイティブアプリのコストは+約0.5秒×1回で有界。副作用の注記: AXEnhancedUserInterfaceは
  ウィンドウマネージャ系ツールで既知の副作用があるが、当該Macでは既にtrueが観測されており新規の
  変化ではない。web test 72件、TypeScript、macOS Debug buildで検証。実画面での候補recall回復は
  再確認待ち。この事象は「AX候補はChrome再起動・自動無効化でいつでも消える」ことを実証したため、
  ブラウザでのrecall安定化（DOM/OCRの精度レイヤー）の優先度判断材料として⑧の計測に含める。

- **仮説**: 1枚の固定画像・同時点のAX候補・会話履歴を単一VLMへ渡せば、モード切替や役職分割なしで
  Spot Visionの回答と画面内案内が成立し、その1ターンを再帰化してCopilotへ発展できる。
- **合格条件**: §5の受け入れ条件 ＋ GA4 golden path（国・地域／デバイス別／同名概要の
  区別を含む20〜30ケース）で誤ハイライト0・誤advance 0・stale採用0。
- **棄却条件（早期判定）**: (a) 正解候補がAXにあるのに画像＋親文脈付きでも頻繁に誤る、
  (b) 正解候補のAX欠落が頻発する、または (c) 新規captureでも進捗判断を誤る場合。死因ごとに
  DOM追加、明示的な画像探索、typed postconditionのいずれか一つだけを次の比較へ加える。
- **実装順**: ①Vision-first品質上限 → ②Luna / `none`比較 → ③固定snapshot単一入力 →
  ④AX候補固定・診断表示・スクリーンショット赤枠 → ⑤実画面で候補recall/selection計測 →
  ⑥不足したセンサーだけ追加 → ⑦クリック後の新capture反復 → ⑧golden path計測。

### 挑戦4以降のバックログ（挑戦3の死因別の分岐）

| 挑戦3の死因 | 次の一手 |
|---|---|
| candidate選択の誤り（候補はあるのに間違える） | C4: Set-of-Marks（番号焼き込み）／F3: Gemini/Claude比較 |
| 候補の欠落（そもそも正解が候補に無い） | B4強化: Chrome拡張のDOMセンサーを精度アドオンとして標準検証／AXEnhancedUserInterface実験 |
| 500件上限による候補欠落 | 低優先度・再発時着手: ローカル全量snapshotとモデル送信subsetを分離／階層圧縮／501・1,500番目回帰テスト |
| 進捗判断の誤り | E4: 主要経路だけtyped postcondition（レシピ）を精度レイヤーとして復活 |
| 目的の見失い・迷走 | D2の強化: 目的+完了済みステップの要約を毎ターン注入／G2: レシピの概略経路提示 |
| 原理的な観測限界（見た目同一） | B6: ユーザー協働（「見つからないので教えてください」UX）／H4: 「違う」ボタン |
| 遅すぎて体験にならない（成立後） | F2: role別軽量化／画像・履歴の段階圧縮（1変数ずつ） |

### 成功の定義（挑戦番号に依存しない）

- ユーザー（当事者）が GA4・freee 級のツールで、マニュアルなしに「開始→光る場所を押す→
  答えを得る」を10回中9回以上完走できる。
- 誤ハイライトによる誤クリック誘発が実用上ゼロ。
- 確証がない時は誤魔化さず止まり、ユーザーがそれを「壊れた」ではなく「正直」と感じる。

---

## 7. 前身文書から引き継ぐ資産（吸収済み）

### 7.1 正のユーザー体験: ナビゲーションモード（2026-07-07 オーナー仕様。エンジンが変わっても不変）

実装・レビューはこの体験に照らして判断する。

> **「開始」を押したら、あとは画面の光る場所を順番に押していくだけで、目的の答えにたどり着く。**

1. 質問すると、回答と一緒に「ナビゲーション開始（Nステップ）」ボタンが提案される。
2. 開始を押すと、パネルは**画面右下の小さな帯**に変形する。帯には「ステップ・目的・現在の
   指示・終了ボタン」だけ。中央の大パネルは出さない。
3. 現在のステップの対象が**実画面上で赤くハイライトされ、消えない**（次のステップまで出続ける）。
4. ユーザーがハイライトされた場所を**自分でクリック**する。ナビゲーション中は
   「◯◯を押す」承認ボタンは出さない（2系統の操作が並ぶと誰も理解できない）。
5. **パネルの外をクリックしてもナビゲーションは終わらない**。クリックを検知したら自動で
   画面を撮り直して進捗を確認する（手動の「撮り直す」も保険として残す）。
6. 最後のステップでは案内ではなく**目的の答えを読み上げて**完了する。
7. 帯の「終了」またはショートカット（右Shift2回）でいつでも離脱できる。

（背景: 従来パネルは「外に触れたら閉じるモーダル」= `handleResignActive` → `closePanel` で、
ナビゲーションと真っ向から矛盾していた。ナビゲーション中だけモーダル性を解除する。
この根本原因はコパイロット帯の設計で回避しており、ソースでは未解決のまま）

### 7.2 有効な製品境界の決定（前身 §8 から。エンジン非依存）

- **マネージャー LLM の常駐はしない**: 往復増・軽快さ原則違反。
- **常時画面監視（先回り）はしない**: ホットキー／ユーザーのクリック起点を堅持。
- **ブラウザ拡張（DOM直読み）は標準経路にしない**: ゼロインテグレーションが差別化の核。
  ただし却下ではなく個社対応パッケージ（アップセル）の選択肢として保持（2026-07-06決定）。
- **ローカル VLM は当面やらない**。
- **再決定（2026-07-15）**: 前身では「構造化出力への全面切替は却下（ストリーミングを失う）」
  だったが、第3次は構造化単一応答を採用する（§5）。案内文のストリーミング表示を
  どう保つか（structured streaming / message 先行出力等）は挑戦3の実装で検証する。
  初トークン体感の要件自体は生きている。

### 7.3 実装上の教訓（前身から）

- **「モデルが何を・コードがどこを」の契約は、コード側の解決が決定論的に正しくて初めて
  成立する**（ラベル解決の段階マッチ「完全一致 > 含む > 部分一致」でも同名重複は救えない
  — だから candidate ID）。
- 履歴に注入した指示はステートレスAPIでは**全ターンに再生**される。初手専用指示の
  履歴混入がマーカー全滅を起こした（第1次）・進捗ターンの1行化を起こした（第2次）。
- クリップボード・パネルフォーカスなど**アプリの外側の状態**が体験を壊すことがある。
  エンジンの精度と切り分けて調査する。

## 8. 本書の運用

- 本書が Navigator/Copilot の唯一の正本。挑戦の仮説・剖検は本書に追記する
  （新ファイルを作らない）。
- 前身2文書（navigator-copilot-plan.md / navigator-stabilization-followups.md）は
  ARCHIVED として old/ へ（経緯の詳細はそちらとgit履歴）。
- 大きな決定は本書と同じコミットで更新する。
