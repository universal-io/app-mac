# GA4を「完全にサポートする」1本目のSkill（R8 M6）

最終更新: 2026-09-03 ／ ステータス: §6の1（probe）完了。用事一覧を実測で訂正済み。器の論点A〜Eをオーナーが決める段階

Skillsの設計根拠は [v3-tool-fit-plan.md](v3-tool-fit-plan.md)、進捗は
[マスタープラン R8](universal-io-master-plan.md)、実装は `api-gateway/lib/server/skills/` が正本。
本書は**1つのツールを選んで徹底的に支え、何が足りないかを実使用で見つける**実証実験の記録と手順を持つ。

## 1. 何を実証するか

汎用の理解だけでは、モデルが一般常識から判定できることしか答えられない。マイナーな機能、
2024年以降に名前が変わった項目、設定がどの画面に隠れているか、はSkillが無ければ常に「分からない」に
なる。**1つのツール（GA4）で「これは完璧に案内できる」と言える状態を一度作る。** それが持続可能な形
（追加コストが定数、他の文脈を壊さない、トークン効率を落とさない、知識を更新し続けられる）で
できるかを、作りながら確かめる。

判定は感触ではなく、§3の用事を実機で流した結果で行う。**作っている途中で器が間違っていると
分かれば、器から作り直す。** 何度もゼロベースへ戻ることを前提にする。

## 2. なぜGA4か（決定済み、2026-09-03）

- 検出が確定的で既に動く（host `analytics.google.com`）。ネイティブアプリのbundle ID衝突
  （システム設定の全ペイン同一ID）がWebには無い
- Chromiumのweb AXで要素が測れるので、AX実測ヒットが取れる。Figma/Canvaのキャンバス、VS Codeの
  エディタ本体（MonacoはAXへ文字を出さない）はヒット無し領域が広く、Skill不足と知覚の限界が混ざる
- 知識がOSに依存しないので、Windows版へそのまま持っていける
- 「見方がわからない」ツールの典型で、解説と案内の両方に需要がある
- 案内の精度問題（開いたプルダウン、メニュー見出しのスコープ）が実際に出た場所で、
  **Skillで直ること・直らないことの境界が最もはっきり見える**
- Stripeは決済・顧客情報が常に画面にあり、案内のプロンプトが「黙って通過させない」と定める種類の
  画面。最初の実験で混ぜない。2本目の候補

## 3. 用事の一覧（golden cases）

起点は特に断りが無ければ「GA4のプロパティを開いた直後のホーム」。オーナーの言葉で書いた用事を
順に並べ、上から試す。**1件ごとに「知識が無い」「知覚できない」「基礎プロンプトの問題」「器の問題」
のどれで落ちたかを記録する。**

凡例 — 種別: 読（解説で答える）／案（案内で画面を動かす）／作（複数手で作る）。
知識: 地図（どの画面にあるか）／概念（数字の意味）／手順（複数手の順序）／権限（必要な役割）。

### 段1: 標準レポートを読む

| # | 用事（ユーザーの言葉） | 期待する到達点・回答 | 種別 | 知識 |
|---|---|---|---|---|
| 1-1 | 直近3ヶ月でアクセスの多かったページはどこ | レポート › エンゲージメント › ページとスクリーン。期間を過去90日に。表示回数順の上位を読む | 案→読 | 地図・概念（表示回数とユーザー数の違い） |
| 1-2 | （1-1の上位ページを指して）このページの流入元はどこ | 同じ表にセカンダリディメンション「セッションの参照元 / メディア」を追加、または 集客 › トラフィック獲得 で「ランディング ページ」で絞る。**ページ単位の流入元はセッション属性であり、page_viewには参照元が無い**ことを説明できるか | 案→読 | 地図・概念（スコープ） |
| 1-3 | このページのデバイス別の内訳 | セカンダリディメンション「デバイス カテゴリ」、または ユーザー › テクノロジー › ユーザーの環境の詳細 + ページで絞る | 案 | 地図 |
| 1-4 | このページの国別の内訳 | セカンダリディメンション「国」、または ユーザー › ユーザー属性 › 詳細（国） + 絞り込み | 案 | 地図 |
| 1-5 | 先月と比べてどう変わった | 右上の期間コントロールで「比較」を有効化。手計算ではなく比較機能へ導く | 案 | 地図 |
| 1-6 | サイト全体でどこから人が来ている | 集客 › トラフィック獲得（セッション単位）と ユーザー獲得（初回接触）の違いを述べ、質問に合う方へ導く | 案→読 | 概念 |
| 1-7 | この数字は何（任意のカードや列を指す） | 指標の定義と、期間・フィルタ・比較・しきい値の影響を述べる | 読 | 概念 |

### 段2: 設定されているものを把握する

| # | 用事 | 期待する到達点・回答 | 種別 | 知識 |
|---|---|---|---|---|
| 2-1 | このサイトで何がイベントとして計測されている | 管理 › データの表示 › イベント。自動収集・拡張計測・カスタムの区別を述べる | 案→読 | 地図・概念 |
| 2-2 | クリックの計測は設定されているか | 管理 › データストリーム › （ウェブストリーム） › 拡張計測機能。「離脱クリック」はここ。任意要素のクリックはGTMなどの外部設定で、GA4画面だけでは確定できないと言えるか | 案→読 | 地図・概念（境界） |
| 2-3 | 何がコンバージョンとして設定されている | 管理 › データの表示 › **キーイベント**（2024年に「コンバージョン」から改名。旧名で聞かれても迷わない） | 案→読 | 地図・概念（改名） |
| 2-4 | このサイトのKPIは何 | 2-3のキーイベント一覧を根拠に「計測上の重要イベント」として答え、事業KPIと計測設定は別物だと断る | 読 | 概念・境界 |
| 2-5 | キーイベントの成果を期間で見たい | レポート › エンゲージメント › キーイベント、または 集客レポートのキーイベント列 | 案 | 地図 |
| 2-6 | どこから来た人がキーイベントに至っている | 集客 › トラフィック獲得 のキーイベント列、または 広告 › アトリビューション | 案→読 | 地図・概念（アトリビューション） |

### 段3: 作る

| # | 用事 | 期待する到達点 | 種別 | 知識 |
|---|---|---|---|---|
| 3-1 | 日常のウェブ解析用に自分のレポートを作りたい | レポート › ライブラリ › 新しいレポートを作成（詳細レポート）。**編集者以上の権限が要る**。無ければclarificationで権限を述べる | 作 | 地図・手順・権限 |
| 3-2 | ページ×国の表を作りたい | 探索 › 自由形式。変数へディメンション・指標を追加 → 設定の行・値へ配置。**置くまで表に何も出ない** | 作 | 手順 |
| 3-3 | ランディングからキーイベントまでのファネルを見たい | 探索 › ファネルデータ探索。ステップの定義 | 作 | 手順・概念 |
| 3-4 | 既存レポートに比較やフィルタを足して保存したい | レポート上で比較を追加 → カスタマイズ → 保存（新規として保存／上書き）。権限 | 作 | 手順・権限 |

### 境界・誤検出・危険操作（Skillが「しないこと」を測る）

| # | 用事 | 期待する振る舞い |
|---|---|---|
| B-1 | しきい値の注記が出ている表で1-1を聞く | 合計と行が一致しない理由として、しきい値を**1件だけ**述べる（列挙しない） |
| B-2 | 編集者権限の無いアカウントで3-1 | 保存できない理由を権限として述べ、迂回を作らない |
| B-3 | Search Console／Looker Studio／GTMの画面 | GA4 Skillが当たらない（誤検出しない）。汎用経路の品質が落ちない |
| B-4 | 管理 › プロパティ設定 › 「プロパティを削除」や データ削除リクエスト | 場所は案内するが、確定はユーザーの決定（clarification）。R16でも道具は押さない対象 |
| B-5 | GA4 Skillが当たらない一般のWebページで1-7のような質問 | 従来の汎用解説と同じ品質（R8共通の受け入れ条件） |

段1が全部通り、段2が通り、段3が通る、の順に「完璧」へ近づく。**段1が通らないうちに段3へ進まない。**

## 4. 現行GA4 Skillの棚卸し（2026-09-03、`api-gateway/lib/server/skills/tools/ga4.ts`）

reading 1,845字、affordances 1,345字、attention 841字。合計約4,000字で、Visionの毎ターン
（案内の進捗ターンを含む）に丸ごと入る。

持っているもの: 左ナビ5区分の意味、プロパティと期間スコープ、ユーザー／セッション／イベントの
スコープ差、ユーザー獲得とトラフィック獲得の違い、探索の変数と設定パネル、(not set)の意味、
セカンダリディメンションと検索、比較、ライブラリ、共有／エクスポート、管理の守備範囲、
しきい値・サンプリング・処理遅延・残留フィルタ・新規プロパティの注意。

§3に対して**無いもの**（机上）:

- **画面の地図**。「ページとスクリーンはエンゲージメントの下」「イベント／キーイベントは管理 › データの表示」
  「拡張計測機能はデータストリームの中」という、区分の1段下の所在が無い。1-1〜1-4、2-1〜2-3は
  ここで落ちる可能性が高い
- **セカンダリディメンションの具体名**（セッションの参照元 / メディア、デバイス カテゴリ、国）。
  「セカンダリディメンションがある」までしか無い
- **改名**。コンバージョン→キーイベントは括弧書き1箇所だけ
- **権限**。ライブラリ保存に編集者以上、という条件が無い（3-1、3-4、B-2）
- **手順**。複数手の順序（探索の作り方、ファネルのステップ定義）を置く節が形式に無い。
  マスタープランは「手順節は別トラック」としているが、段3はこれなしに成立しない
- **境界**。「GA4画面だけでは確定できない」（2-2のGTM側設定、2-4の事業KPI）を言うための知識

**Skillでは直らないと分かっているもの**（混同しないこと）:

- 開いたプルダウンの中身が読めるか — モデル依存（`gpt-5.6-luna`で読める。guidance-accuracy-plan 1-m）
- メニューの見出しが配下項目のスコープを決めることを無視する — 基礎プロンプトに置くかSkillに置くか
  **未決**（同 1-n）。管理メニューが深いGA4では必ず当たる。ここで出たら、その回で決める
- 進捗ターンが前の画面を持たない、候補ゼロが成功経路を流れる — R12の範囲

## 5. 器の論点（オーナーが決める）

「完璧」をいまの3本の文字列に詰めると、1ターンあたり数万字になり、他文脈の劣化とトークン効率の悪化が
起きる。着手前に決めるのは次の5点だけで、M7のカタログ基盤（registry生成、段階公開、rollback）には触らない。

| 論点 | 選択肢 | 推奨 |
|---|---|---|
| A. 画面moduleへの分割 | (1) 1製品1文字列のまま ／ (2) 製品共通＋画面module（レポート／探索／管理／広告）で該当1〜3個だけ注入 | **(2)**。v3計画§3の構想を1製品で先に実装する。1ターンの注入量を製品共通≤2,500字＋module≤3,000字程度に抑える |
| B. moduleの判定シグナル | (1) ウインドウタイトル ／ (2) AX候補のラベル（左ナビの選択状態、見出し）／ (3) モデルに選ばせる | **(1)＋(2)をGateway側で決定的に**。(3)はv3計画が禁止。GA4のタイトル書式は未測定なので**最初のprobeで測る** |
| C. 手順節の形 | (1) 自由文 ／ (2) 構造化データ（用事→前提・権限→手順の列）を注入時に文へ描く | **(2)**。golden caseと1対1に照合でき、改名や画面変更で差分が追える。安全性は載せない（Skillはデータ） |
| D. 出所とversion | (1) 持たない ／ (2) module単位に出所（公式ヘルプのURL・確認日）とversionを持つ | **(2)**。「知識を更新し続ける仕組み」の本体は後回しでよいが、更新できる形は最初から |
| E. 検証に使うプロパティ | (1) オーナー自身のプロパティ ／ (2) Google公式のデモアカウント（Merch Shop） | **両方**。(1)で2-4（KPI）のように所有者しか判定できない用事を、(2)で他人と再現できる用事を測る。(2)の利用可否は要確認 |

判定基準は3つ。**Skillが当たらない画面で汎用品質が落ちない**（R8共通）／**1ターンの注入量が
用事の総数に比例しない**（module分割が効いている証拠）／**GA4の2本目の画面moduleを足すコストが
1本目と同じ**（定数）。

## 6. 進め方

1. **probe（実機、リポジトリ外）**: GA4をChromeで開き、レポート／探索／管理の各画面で
   `window_title`・`host`・AX候補ラベル（左ナビ・見出し）を実測する。論点Bの材料。シェルからAXは
   読めるので、オーナーは画面を開くだけでよい
2. **現行Skillのままベースライン**: §3を段1から順に実機で流し、落ちた理由を4分類で記録する（§7）。
   **Skillを書く前に測る**。書いてから測ると「何が効いたか」が分からない
3. 論点A〜Eの決定を受けて器を直す（`types.ts`／`registry.ts`／注入関数。Gatewayデプロイ＝本番）
4. 「知識が無い」で落ちた用事だけをSkillへ書く。1件ずつ、書いたら同じ用事を流し直す
5. 段1が通ったらB-3・B-5（汎用非劣化）を流し、段2へ

各段の終わりに§7へ結果を記録し、器を直したらその理由を§8へ残す。

## 7. probeの結果（2026-09-03、§6の1）

Chromeで開いた実プロパティ（アカウント `www.workflowdesign.net` ／ プロパティ `whatif-ep.xyz`）を、**本番の
`VisionObservationCaptureService` と同じ規則**（候補13ロール、ラベル優先順、parentLabel継承、states）で
26画面測った。道具と生データは `/private/tmp/ga4probe-20260903/`（`probe.swift`、`signals.json`、`dumps/`）。
画面遷移はAXの`AXPress`で行い、スクロールはCGEventのホイールを使った。

### 7-1. 判明した事実（すべて実測）

- **ウインドウタイトルは26画面中5画面しかレポート名を持たない。** 「アナリティクス | ホーム」「アナリティクス |
  ページとスクリーン: ページパスとスクリーン クラス」の形はホームとレポート表示中だけで、残り21画面は
  「アナリティクス」のみ。**タイトル単独では画面を判定できない。**
- **`detect`のタイトル一致は日本語UIでは一度も当たらない。** 26画面のどのタイトルにも "google analytics" は
  含まれない（`matchesProduct`は死に規則で、実際は`host`だけが効いている）。逆にLooker StudioやGTMの
  タイトルに "Google Analytics" が入ると誤検出し得る。
- **レポート左パネルの構成はプロパティ設定で変わる。** このプロパティはライフサイクル（集客／エンゲージメント／
  収益化／維持率）が**非公開**で、公開されているのは「データ インポート」「ビジネス目標」「ユーザー」。
  「トラフィック獲得」「ユーザー獲得」は**ビジネス目標 › 見込み顧客の発掘**の中にある。同じ「ページとスクリーン」が
  2つのグループに重複して現れる。**Skillの地図を「親ノード名」で書くと、このプロパティでは嘘になる。**
- **「キーイベント」は管理の独立項目ではない。** 管理 › データの表示 › **イベント**（`/admin/events/hub`）の中の
  タブ（radiobutton「キーイベント」／「最近のイベント」）。計画書§3の2-3の期待経路は古い。
- **画面外の要素はAXツリーに存在するが、候補にならない。** 管理画面でホイールを回すと候補linkが28→48件へ増える
  一方、**走査ノード数は641→655（+14）しか増えない**。増えた20件のlinkは、スクロール前のダンプにも
  非候補レコード（AXRow）として全件存在した。落としているのは`normalized()`の「矩形がcaptureRectと交差しない」判定で、
  **AXが隠しているのではない。** つまりクライアント側で`offscreen`として送る設計が原理的に可能。
- **候補の47%はChrome自身のUI。** 26画面すべてで**きっかり57件**（ブックマーク22、タブ19、拡張機能、
  アドレスバー等）。AXWebAreaはy=154から始まり、57件は全部その外側。**この57件には他サイトのフルURL・
  メールアドレス・タブ題名が含まれ、毎ターン送られている** — GA4のURLパスを方針で伏せている一方で、である。
  旧ユニバーサルアナリティクスのブックマークも混ざる。
- **`collapsed`は情報量ゼロ。** GA4本文側候補1,688件のうち1,665件（98%）に付く。`expanded`が付くのは23件だけで、
  ほぼ「ナビゲーション切り替え」。**開いているコレクションにも閉じているものと同じ`collapsed`が付き、
  表示中のレポートに`selected`も付かない** — 現在地と開閉はstatesから読めない。
- **同一の候補が重複する。** ページとスクリーンで175件中41件、トラフィック獲得で39件が(role,label,矩形)の重複
  （GA4の表が列ヘッダを2つの親から二重に露出）。
- **セカンダリディメンションの「+」はAXに無い。** 主ディメンションのセルは x459-731（ラベルとプルダウン矢印）で、
  その右の x731-763 に候補が無い。**1-2〜1-4は枠で指せず、言葉だけの案内になる。**
- **探索の「空白」は押した瞬間に無題のデータ探索を作って保存する**（URLが`/analysis/<prop>/edit/<id>`へ変わる。
  probeで1件作った）。タイトルは「アナリティクス」のままなので、一覧とエディタはタイトルで区別できない。

### 7-2. 論点Bへの答え: module判定は現行契約のまま成立する（26/26）

**本番で届くもの（`window_title`と候補の`role`/`label`）だけを使う規則を書き、26画面すべてで正解した。**
矩形・AXHeading・URLは使っていない。Chrome自身のUI 57件だけを与えると`UNKNOWN`に落ち、誤発火しない。

| module | 規則（本番シグナルのみ） |
|---|---|
| `home` | タイトルが `アナリティクス | ホーム` |
| `reports.report:<名>` | タイトルに ` | ` があり、ホーム以外。`<名>`は `:` の手前 |
| `explore.list` | 候補ラベルに「空白」「自由形式」「ファネルデータ探索」「経路データ探索」のうち3つ以上 |
| `explore.editor` | 「手法を選択」「設定を最小化」「[変数] パネルを最小化」「ビジュアリゼーション」のうち2つ以上 |
| `admin.events` | 「イベントを作成」「アトリビューション設定」「DebugView ページに移動」のうち2つ以上 |
| `admin.streams` | 「ストリームを追加」または「ストリームを選択」があり「データ ストリーム」もある |
| `admin` | 「アカウント設定」「プロパティ設定」「データの収集と修正」のうち2つ以上 |
| `reports.library` | 「新しいレポートを作成」または「新しいコレクションを作成」 |
| `advertising` | 「広告スナップショット」または「広告レポートを取得」 |
| `tasks` | 「アカウントをリンクする」「レポートを強化する」「操作を実行」のうち2つ以上 |
| `reports.start` | 「レポートのスナップショット」と「ライブラリ」が両方（上のどれにも当たらない時） |

**壊れる条件（未測定・要注意）**: ①英語UIなど他言語 — 語彙が全部日本語なので言語別の語彙パックが要る
（構造規則（タイトルの` | `分割）だけは言語非依存）。②左パネルを折りたたんだ状態 — `reports.start`と
`admin`の判定材料が消える。③Safari — タイトルとAX構造が違う。④未測定の管理下層（データの保持、
オーディエンス、カスタム定義、DebugView等）は`admin`止まりで、下層ごとの語彙は1画面ずつ測って足す。

**契約への追加は不要。** GA4のURLはパスが常に `/analytics/web/` で、実体はフラグメント側にあるため、
「パスを送る」を許しても何も得られず、フラグメントを送ればプロパティIDが漏れる。**hostのみの方針は
変えなくてよい。** 足すとしたら安いのは `page_language`（`AXLanguage` 1属性）で、語彙パックの選択が
決定的になる。次点は「最初の可視AXHeadingを1行」だが、上記26/26が出た以上、今は要らない。

### 7-3. §3の期待経路の訂正

| # | 計画書の期待 | 実測での経路 | 状態 |
|---|---|---|---|
| 1-1 | エンゲージメント › ページとスクリーン | ビジネス目標 › ウェブ / アプリのトラフィックの分析（または ユーザー エンゲージメント…） › ページとスクリーン | プロパティ固有 |
| 1-2 | セカンダリに「セッションの参照元 / メディア」 | 「+」がAXに無い。代替は 見込み顧客の発掘 › ランディング ページ | 要修正 |
| 1-3 / 1-4 | セカンダリ、または ユーザー › テクノロジー／ユーザー属性 | 「ユーザー属性の詳細」が ウェブ / アプリのトラフィックの分析 の直下にある | 名称違い |
| 1-5 | 期間コントロールの比較 | combobox「期間選択ツールを開く」（右上）。**同じ画面の「比較を追加」はセグメント比較で別物** | 一致（混同注意） |
| 1-6 | 集客 › トラフィック獲得／ユーザー獲得 | 見込み顧客の発掘 › トラフィック獲得／ユーザー獲得。「集客」という節は無い | プロパティ固有 |
| 2-1 | 管理 › データの表示 › イベント | 同じ。ただし**初期表示では候補に無く、スクロールが要る** | 一致（知覚に条件） |
| 2-3 | 管理 › データの表示 › キーイベント | 管理 › データの表示 › イベント の**タブ**「キーイベント」 | 古い |
| 2-5 | エンゲージメント › キーイベント | そのレポートは無い。ホームの「キーイベント」タブか、集客系レポートのキーイベント列（**横スクロール**） | プロパティ固有 |
| 2-6 | 広告 › アトリビューション | Google広告未リンクで「広告掲載を始めましょう」。到達不能 | 到達不能 |
| 3-1 | ライブラリ › 新しいレポートを作成 | 一致（レポート左パネル最下部「ライブラリ」→ popupbutton「新しいレポートを作成」） | 一致 |
| 3-2 | 探索 › 自由形式 | 一致。ただし押した瞬間に無題の探索が作られる | 一致（副作用あり） |
| B-1 | しきい値の注記 | このプロパティでは出ない見込み（データ品質は「100%」、Googleシグナル未有効） | 測れない |
| B-2 | 権限不足 | 所有者アカウントのため再現しない | 測れない |

**§5-Eの結論**: しきい値、権限不足、広告アトリビューションは**このプロパティでは測れない**。
デモアカウント（または権限を落とした別アカウント）が要る。

### 7-4. 机上ベースライン予測（実機で流す前の見立て）

| 段 | 予測 | 主な理由 |
|---|---|---|
| 1-5 / 1-7 | 通る | 現行Skillの概念知識で足りる |
| 1-1 / 1-6 | 落ちる（知識） | 左パネルの地図が無い。「集客」を言うと外れる |
| 1-2 / 1-3 / 1-4 | 落ちる（知識＋知覚） | セカンダリの「+」がAXに無く、代替経路の知識も無い |
| 2-1 〜 2-6 | 落ちる（知識＋知覚） | 管理のスクロール、キーイベントのタブ化、列の横スクロール |
| 3-1 〜 3-4 | 落ちる（器） | 複数手の手順を置く節が形式に無い |
| B-4 | 落ちる（基礎プロンプト） | 削除系がclarificationの列挙に入っていない |

### 7-5. 現行 `ga4.ts` の誤りと不足

**誤り**: 左ナビを5区分としている（実際は6項目で「タスク」がある。順序も違う）／期間コントロールを
「右上」と断定（探索エディタでは左の変数パネル内）／「conversions (key events)」を管理の独立項目のように書いている
（実際はイベントのタブ）／「ライブラリへ保存」の方向がずれている（保存はレポート上の「レポートをカスタマイズします」から）。

**不足**: 画面の地図（コレクション構造と公開・非公開、管理の5セクション、探索ハブの4ボタン）／
操作語彙の正確な文字列／権限（ライブラリ保存に編集者以上）／複数手の手順／
「statesの`collapsed`は無意味」「現在地はstatesに出ない」「Chromeのブックマークとタブは画面の一部ではない」という**読み方の規則**。

### 7-6. Skillでは直らないもの（R12・クライアント側へ）

1. **Chrome UI 57件の混入 — 2026-09-03に修正済み。** 1ターンあたり約3.7kトークン、候補の47%。
   私的情報（他サイトのURL、メールアドレス）を毎ターン送っていた。**web areaを見つけた窓では、
   その配下の要素だけを送る**ようにした（`VisionCandidateScope.inTool`）。同日の実測で、Slack 95件・
   VS Code 197件は**全部がweb areaの中**なので何も減らず、Finder・Xcodeはweb areaを持たないので
   従来どおり。ブラウザ自体の操作（戻る・タブ・アドレスバー）は指せなくなる。
2. **`collapsed`の乱発 — 2026-09-03に修正済み。** `expanded`は真の時だけ、`collapsed`は
   AXDisclosureTriangleだけに限った（`VisionCandidateScope.expansionState`）。1,665件→ほぼ0件。
3. **重複候補**（レポート画面で約40件）。(role,label,矩形)で一意化すれば消える。
4. **画面外要素**。ツリーには居るので、`offscreen`状態を付けて送れば「下にあります」と案内できる。
   ただし進捗ターンの起点は`leftMouseUp`だけなので、**スクロールでは撮り直しが起きない**。
5. **`detect`のタイトル一致が死んでいる**（日本語UI）。誤検出のリスクだけが残っている。

### 7-7. 論点A〜Eへの含意

- **A（module分割）**: 推奨どおり分割する。実測で11 moduleに分かれ、判定も成立した。
- **B（判定シグナル）**: **契約変更なしで確定できる**（26/26）。ウインドウタイトル＋候補ラベル。
  モデルに選ばせる必要は無い。言語別の語彙パックだけ形式に用意する。
- **C（手順節）**: 必要性が確定した。段3は手順節なしには成立しない。
- **D（出所とversion）**: 必要性が確定した。「キーイベント」の改名のように、**製品側の変更が知識を古くする**。
- **E（検証プロパティ）**: **デモアカウントが必須**。しきい値・権限・広告はこのプロパティでは測れない。

### 7-8. 実測記録（用事ごと）

| 日付 | # | 結果 | 落ちた理由（知識／知覚／基礎プロンプト／器） | メモ |
|---|---|---|---|---|
| | | | | まだ実機で流していない（§6の2） |

## 8. 器を直した記録（ゼロベースへ戻った理由）

（未記入）
