# セッション引き継ぎ

**この1本だけを、常に次のセッションのためだけに保つ。** 役目を終えた記述は消し、増やさない・
分けない・積み上げない。現在の設計は [README](README.md) と
[docs/universal-io-master-plan.md](docs/universal-io-master-plan.md) が正本で、ここはその上に乗る
「いま何をすべきか」だけ。

最終更新: 2026-09-06

---

## ⚠️ 最初に読む: 2026-09-06の破綻は原因が確定し、修正済み。**実機はまだ**

**症状**（Xcodeから起動したDebug、VS Codeの`README.md — app-mac`で右Shift 2回、案内へ）: ①案内に入っても枠が出ない
（候補0件）②赤い「接続に失敗しました … Canceled」③撮影（走査線）が連続する。

**原因は3つとも確定した**（unified log 18:27〜18:30の`capture.display`／`vision.axCollected`／DEBUG NSLogの各パス、シェルからの
AX実測、コード）。**どちらのコミットも戻していない。** `1a02a77`（候補フィルタ）は無関係と確定、`c41da09`（ループ）は3箇所を直した。
**修正はブランチ`fix/guidance-loop-20260906`にあり、チェックアウト中**（Xcodeから起動すればこのブランチが動く）。218 unit test。
下の6項目を実機で見てから`main`へ入れる — 「一度も画面に出していないものを`main`に入れない」を今回は守る。

| 症状 | 原因 | 直したもの |
|---|---|---|
| ①候補0件 | **ディスプレイの不一致。** このMacは2画面で、VS Codeは2番目（CG 1728,30 1920×1050）、Chromeは1番目にある。この日の撮影は**全部`display=1`**。AX走査は前面アプリ＝VS Codeの木（6,132ノード、web areaあり）を歩くが、要素の矩形は全部撮影矩形の外なので`normalized`で落ちる。「complete」で0件になる。**probeで同じ規則をdisplay 1の矩形に当てるとVS Codeは0件（offRect=291）、display 2の矩形なら70件。Chromeはnodes=240／42件／chrome=57で本番ログと桁まで一致** | (a) 走査の前にウインドウ矩形と撮影矩形の交差を見て、無ければ歩かず`window_off_capture`を記録（診断レポートの`status`と`vision.axCollected truncated=`。`AXTruncationCode`に追加）(b) summon時のディスプレイ決定`ActiveDisplay.pin`に、AXが答えない時の予備としてウインドウサーバー（`CGWindowList`）経由を足した。ポインタは最後の手段。**`display.pinned via=focusedWindow\|frontWindow\|pointer display=N`を毎回記録** |
| ②赤い「Canceled」 | 打ち切られた手のGateway往復が`URLError.cancelled`（-999）で終わる。これは`CancellationError`**ではない**ので`evaluateCopilotProgress`の汎用catchに落ち、`copilot.failed`＋赤字＋`.timedOut`を**後続の手の上に**書いた（log: `guide.act.superseded`の70ms後に`copilot.failed error=transport.-999`） | `evaluateCopilotProgress`に世代（`copilotStepGeneration`）を渡し、catchの先頭で`generation`不一致または`Task.isCancelled`なら黙って抜ける（一時ファイルだけ消す） |
| ③連続撮影 | (a) 候補0件で対象が無い→スクロール停止ごとに撮る（①の派生）(b) エディタのカーソル移動クリックは`AXTextArea`＝入力欄→**クリックの0.8秒後に毎回撮る**（何も打っていなくても）(c) バブル上のスクロールを除外していない | (b) **入力欄クリックは時計を始めない。最初のキーで始まり、以後のキーで押し戻す**（`GuidanceTrigger.restartsTypingIdle`、純粋関数・テスト2本）。打たずに離れた欄は操作に数えない。前の欄の入力で走っている時計は止めないので、素早く2欄埋めれば1手。GA4のフォーム（打って次へ）は従来どおり欄ごとに1手、エディタのカーソル移動は0手 (c) スクロール監視にクリックと同じバブル除外（`advancesGuidance`） |

**なぜdisplay 1に固定されたか（最有力・未確定）**: 18:27:59の初回AX走査が2パスとも**7ノード**だった。VS Codeの木が冷えていて
`AXFocusedWindow`が0.2秒以内に答えず、`pin`がポインタの画面へ落ちた、と読める。今のVS Codeは温まっていて再現できない。
次回は`display.pinned`の記録で確定する。

**捨てたもの・変えていないもの**: 案内は**summon時の画面に固定**のまま（撮影・枠・バブル）。案内中に別画面のアプリを
クリックしても追従しない。追従させるなら幕・枠・バブルの窓を作り直す構造変更なので、**設計の論点としてオーナーへ**。
今は`window_off_capture`で「別画面だった」と記録が残るだけ。

### 実機で確認すること（この順。Xcodeから起動、2画面のまま）

1. **VS Code（2画面目）で右Shift 2回。** `display.pinned … display=2`が出て、幕がVS Codeの画面に乗るか。もし`via=pointer`なら
   VS CodeのAXが冷えていた証拠で、`frontWindow`が拾えなかった理由を見る
2. **案内へ入り、エディタ内を数回クリック。** 走査線が**走らない**こと（`guide.act.deferred`だけが並ぶ）。キーを打って手を止めると
   0.8秒後に**1回だけ**走ること（`guide.act.resumed into=typingIdle`）
3. **バブルのスレッドをスクロール。** 走査線が走らないこと
4. **評価中にクリック。** `guide.act.superseded`が出て、**赤字が出ない**こと。新しい手の指示が出ること
5. **GA4（1画面目）で回帰。** 候補76件前後、案内が4手で完走（2026-09-03と同じ）
6. **VS Codeで案内中にChrome（1画面目）をクリック。** 診断レポートの`status: window_off_capture`。上の「設計の論点」の実物

```bash
/usr/bin/log show --last 1h --predicate 'eventMessage CONTAINS "guide." OR eventMessage CONTAINS "display.pinned" OR eventMessage CONTAINS "vision.axCollected" OR eventMessage CONTAINS "copilot."'
```

### この2日で正しかったこと（捨てないもの）

- GA4の実測26画面と、module判定が現行契約で26/26成立すること（`docs/ga4-complete-skill-plan.md` §7）
- 1-2の欠陥は「このページ」の指す先が進捗ターンに送られないこと（1-8で切り分け済み）。1-1は知識（概念）
- 案内ループの4つの欠陥（L1〜L5）の機序と、unified logの`guide.act.folded` 7件/10ターンという証拠
- 「枠の現在性」と「指示の現在性」は別の問題で、前者はAX位置の再読（0.13ms）で撮影なしに解ける、という切り分け
- ブラウザ部品57件の除外（GA4で実機確認済み。私的情報の送信を止めた）。**VS Codeの0件はこの除外のせいではなかった**

### 教訓（記憶にも書いた）

- **候補0件はまず「どの画面を撮ったか」を疑う。** `capture.display display=N`とアプリの窓の位置を並べる。probeは窓の矩形で
  判定していたので、本番だけの差＝撮影矩形が見えなかった
- 実機不具合の一次証拠はunified log。今回は`log show`の`Vision AX collection pass=`（DEBUG NSLog）にnodes=7／1393／240／6132が
  そのまま残っていて、シェルからのAX実測（`CGWindowList`で窓の位置、probeで各ディスプレイ矩形との交差）と突き合わせて確定した

---

## いまどこにいるか

**Visionは「解説」と「案内」の2状態で完成し、両方が実機で動いている。** 右Shift 2回で実画面に幕が乗り、
入った瞬間に解説が始まる。クリックすればその場所、囲めばその範囲。質問を打ち、その回答が次の一手なら
**幕が引いて案内に入り**、次に押す対象に枠が出て、クリックは対象アプリへ通る。押すと画面を撮り直して
次の一手が出る。バブルの中はメッセンジャー型のスレッド（R17、実機確認済み）。216 unit testが通り、
`v0.2.3` build `10`として公開済み（`main`はその後R17分が4コミット先）。

**動いた証拠**（unified log、2026-08-25 21:43〜21:51）: 3セッションとも
`guide.entered via=copilotAutoStarted` → `copilot.capture` → `copilot.turn` と進み、1回は2手で
`guide.done state=complete`。`guide.left via=cross` で解説へ復帰も確認。指した対象の認知精度は
100%（2026-08-24）。

**設計の正本は [マスタープラン](docs/universal-io-master-plan.md) の「製品」「現行アーキテクチャ」**
（＝仕様）。経緯と実測は同じ文書のR14・R15。**もう「次にやること」ではない。**

**実測**: 質問→案内入場まで約4.6秒、1ステップ（クリック→次の一手）が約6〜10秒
（撮影1.7〜4.4秒＋Gateway往復3.5〜4.3秒）。

---

## 次のセッションが選ぶもの

R14/R15/R17は終わった。候補は2つで、**どちらも独立している**（ほかに「別セッションで扱うと決めたもの」が
下にある）。**R16だけは、着手すると製品の段が1つ上がる。**

**閉じたもの（2026-09-03）**: R17（バブルの対話スレッド）— 実装と同日の見た目修正3件（顔を1枚・1部品・1径、
状態行の説明文削除、チップ「案内中」＋脈動）、印の根拠の改訂（囲み・AX実測ありでは回答枠を採用しない）、
格子の重力撤去まで実機確認済み。案内の状態チップと状態行の見た目（R15で後回しにしていたもの）も
課題として終了した。仕様は[マスタープラン R17](docs/universal-io-master-plan.md)。

**閉じたもの（2026-09-03）**: F12 — `v0.2.3` build `10`を公開ダウンロードへ切り替えた（ソース`db1feba`、
タグ`v0.2.3`。build `9`は候補ソースの後に修正が十数コミット入ったため公開せず破棄）。
Composeのバブル化（2026-08-27実装）は同じ署名ビルドで実機確認済み。
実装の詳細は[README](README.md)「現行機能」とマスタープランR14。

- **D7（24時間稼働のgolden path）は2026-08-31に毎リリースの必須ゲートから外れた**（オーナー判断）。
  次に実施する条件は[manual-golden-paths.md](docs/manual-golden-paths.md)。**過去2回の試行は
  どちらも実際に欠陥を見つけている**ので、条件に当たるリリースでは省かないこと。
- **入場ms・クリックms・AXヒット率のunified logからの集計は未実施**（F12の残り物。小さい）。

### 1. スキル（注入できる文脈）が圧倒的に足りない — **着手中（2026-09-03、GA4で）**

**案内はスキルが無いとあまり機能しない。** 解説対象に対して用意できているスキルの数が圧倒的に
不足している。**回路が開いた今、案内の質を決めるのはモデルではなくここ。**

**進め方はオーナーが決めた**: ツールを1つ選び（GA4）、そのツールを「完璧に案内できる」状態を一度作って、
何が足りないかを実使用で見つける。器（Skillの形式）が間違っていれば器から作り直す。正本は
[ga4-complete-skill-plan.md](docs/ga4-complete-skill-plan.md) — 用事の一覧（§3）、現行Skillの棚卸し（§4）、
器の論点A〜E（§5）、手順（§6）、**probeの結果（§7）**、記録欄（§7-8・§8）。

**§6の1（probe）は完了（2026-09-03）。** 実GA4を26画面、本番と同じAX規則で測った。道具と生データは
`/private/tmp/ga4probe-20260903/`。**論点Bは解決** — ウインドウタイトル＋候補ラベルだけで画面moduleを
11種類に判定でき26/26正解、Chrome UIだけでは誤発火しない。**契約（送るものの取り決め）の変更は不要。**
§3の期待経路は5件が古いかプロパティ固有だった（キーイベントはイベント画面のタブ、左パネルはプロパティ設定で変わる）。
**しきい値・権限不足・広告アトリビューションはこのプロパティでは測れない**ので、論点Eのデモアカウントが必須。

**§6の2（ベースライン計測）の前に、オーナー判断でクライアント側の雑音を先に直した（2026-09-03、206 unit test）。**
`VisionCandidateScope`（純粋判定・移植単位）を新設し、①ブラウザの窓ではページの中の要素だけを送る
②`collapsed`は`AXDisclosureTriangle`だけ、を実装。**同日、実機で4点とも確認済み**:
GA4ホームが132→**76件**（ブラウザ部品57件だけが消え、ページ側は無傷）／案内が**4手で完走**（オーナーの体感で精度も向上）／
Xcodeは**58件・`web_area_present: false`**でprobe実測値と完全一致（ネイティブは無影響）／
**リロード直後でも143件**で、心配していた「ページが間に合わず0件」は起きなかった（`collection_passes: 2`で間に合う）。

**バブルのスレッドをコピーできない**（オーナー指摘、2026-09-03）。案内の記録を残すのに毎回スクリーンショットが要る。
覆いの上でのテキスト選択は2026-08-24に撤退済みなので、**選択ではなく「スレッドをコピー」ボタン**（情報ボタンの隣か中）で
解く。急ぎではないが、ベースライン計測の記録コストに直接効く。

**§6の2（ベースライン）は段1を5件流した（2026-09-03、§7-8）。** 通った: 1-6、1-7、1-8。欠陥: 1-1（知識・概念: 表示回数を
言いながらランディング ページを選んだ）、1-2（**「このページ」の指す先が進捗ターンに送られない** — クライアント）。
**流している間に案内ループの課題が4件出た（§7-9、L1〜L4）。** L1（入力中に手が始まる）とL2（評価中のクリックが捨てられ、
古い枠が残る。unified logで7件/10ターン）は同じ根。オーナーが「フレーム限定の起点は行き止まり／裏で何度呼んでもよい」と前提を出したので、**ループの再設計案を§7-10に書いた**
（枠の現在性はAX位置の再読で局所に解く、指示の現在性はイベント駆動＋打ち切り、常時撮影は不要）。**オーナーが採用（「撮影は少ないほど良い。AXとイベントで分かるなら撮らない」）→同日実装、216 unit test。
2026-09-06の実機で破綻（冒頭の⚠️節）。**
実機で見る4点は§7-10。probeで確定した事実: AX位置読み0.13ms、フォーカス役割の読み0.16ms（クリック後200ms以内に観測）。
未確定: `keyDown`グローバル監視がChromeで届くか（届かなければ入力欄で待った後の再開が起きない — 記録の`resumed`で分かる）。 段1の残り（1-3〜1-5）と段2は、
ループを直してから流す方が記録が汚れない。

現状の事実（調査 2026-09-03）: Skillは`api-gateway/lib/server/skills/`のTypeScript 5本（Slack、Gmail、GA4、
Stripe、アクティビティモニタ）で、DBも管理画面もAPIも無く、追加＝本番デプロイ。業務層・個社層は空。
Composeレビュー経路にはSkillが入っていない。評価基盤は検出のunit test 4本のみ。足りないSkillを知る
テレメトリも無い。**残るクライアント側の欠陥3件**（重複候補、画面外要素をofscreenとして送れるか、
日本語UIで死んでいるタイトル判定）は§7-6。

**案内の評価はスキルの薄さと切り離せない** — 案内が下手なのか、渡す文脈が無いのかを混同しないこと。
設計の枠組みは [v3-tool-fit-plan.md](docs/v3-tool-fit-plan.md)（精度レイヤー＝Skills、注入専用データ、
自動判別＋手動カスタマイズ、個社＝スキルセット定義）。

### 2. R16 — 道具が押す（2026-08-30 設計確定・未着手）

**案内は「押してください」と枠を出す。R16はそれを「押します」に替える。一手ごとにユーザーが確定する。**
設計の正本は[マスタープラン R16](docs/universal-io-master-plan.md)。同日、「## 製品」の背骨の一文を
`自律操作ではなく、最終判断と操作はユーザーが行う` から**一手ごとの確定**を線にする形へ書き換えた
（この粒度だけが「操作」を許し、その先の「連続」「無人」を許さない。なお**「連続」までは個人ユーザーの
道具**で、別製品になるのは「無人」だけ）。`api-gateway/docs/design-philosophy.md`の
「AXクリック（実装済み）」も事実に合わせて直した。

着手前に知っておくべき3つ:

- **作動層は既にある。呼び出し元がゼロなだけ。** `BombSquad/Services/AXActionService.swift` は
  Navigator時代の`821f6cb`で入り、`5e42145`（経路一本化）で呼び出し元だけが消えた。
  **作動が失敗して捨てられたのではない。** 当時の体験は`git show 5e42145`に全部残っている。
  ただし**そのまま復活させてはいけない**（モデルの推定矩形へ盲打ちする第3経路がある／成功判定が
  戻り値だけ／対象をラベル文字列で引く＝2026-07-06のバグの温床）。受け継ぐのは2つ——
  **要素ハンドルをキャッシュせず確定時に引き直す**のと、**可視要素には合成クリックをAXPressより優先**する
  （AXPressは成功を返しながら何も起きないことがある＝**戻り値は成功判定に使えない**）。
- **挿入点は1点だけで、押した後の経路は人が押した時と完全に同じ。**
  `evaluateCopilotProgress`の`.guide`分岐で`installCopilotClickMonitor()`を呼んでいるまさにその行を
  確定待ちに替え、確定したら押して`scheduleCopilotProgressCheck`へ合流する。変化待ち撮影・進捗ターン・
  `maxGuideSteps=15`の止め弁・診断はすべて無改造で通る。
- **最優先probeは自己ループ。** 案内中の監視は`.leftMouseUp`をグローバルに拾い、除外条件は
  「バブル矩形の内側か」の2行だけで**出所を見ていない**。合成クリックは`.cghidEventTap`へ投函するので
  自分の監視へ返る可能性が高く、返れば1手で二重に撮影・二重に課金する。
  **「自アプリのイベントは届かない」はこの製品で既に一度実機に反証されている。**

**G0（借金）は今すぐ効く**: 座標変換の式が3つある。`VisionPointerResolver`の2関数に加え、
`SessionCoordinator.cocoaFrame(fromAXFrame:)`（`main`にコミット済み）があり、
主ディスプレイ高さの取り方が2通りになっている（`NSScreen.screens.first?.frame.maxY` と
`CGDisplayBounds(CGMainDisplayID()).height`）。実用上は同値だが、HANDOFFが「3つ目の式を書くと
二次ディスプレイでずれる」と名指した種類の重複がまさに増えた直後である。R16はAX frame→実画面の変換を
必ず使うので、着手前に1本へ戻す。

---

## 精度の問題は解決した（2026-08-24）

**実機で100%の精度で対象を認知している。** 経過と、次に同種の問題を追う時のために残す事実:

1. **送信バイトを見る道具を作った**（`GatewayVisionClient.dumpWireImage`、DEBUG限定・XCTest時は書かない）。
   `/tmp/universal-io-wire-dumps/` に、Gatewayへ送るまさにそのバイト列が
   `wire-<時刻>-x<千分率>-y<千分率>-burned.png` として残る。app-iosが1枚で決着させた道具
   （`../app-ios/docs/investigation-highlight-offset.md` §3）で、**これを最初に作るべきだった**
2. **8クリック分の実測で、クライアントは完全に無罪だった。** `burned=true` 8/8、送信寸法1728×1117、
   `display=1`固定。GitLabの右上＋を押した回の画像を開くと、マゼンタのリングは＋の**真上**に乗っていた。
   つまり座標変換・焼き込み・ディスプレイ選択のどれも壊れていない
3. **真因はモデルの取り違えだった。** 画面に見た目の似た＋が2つあり、モデルはリングを見た上で
   意味的に似た別の＋を主題に選んでいた（「5個飛び越える」に見えた症状の正体）
4. **効いた修正は、AXが実測した要素の名前を渡すこと。** `input.pointer.hit_candidate_id`（Gateway
   `08e570e`で本番配備済み）が候補リストのidを名指しし、プロンプトが「マークとOS実測は同じ場所を
   指している。似た見た目のコントロールが複数あるとき、ユーザーが指したのはこの実測要素であり
   他所のそっくりさんではない」と固定する。クライアントは`VisionPointerResolver.candidate(at:in:)`が
   既に持っていたヒットのidを載せるだけ（`hit`が無ければキーごと不在＝リクエスト不変）

**`hit=false`の画面（AXが何も測れない本文・画像・グラフ）では、この手は効かない。** そこはマークだけの
勝負に戻るので、精度が落ちる画面があるとすればそこである。

## 囲んで聞く（実装済み・実機確認済み）

**2026-08-24に復活させた。** 覆いの上でドラッグすると軌跡がその場に描かれ、離した時点で
「この範囲について」の質問になる。

- 判定は**パスのバウンズ**（長辺8pt以上で囲み）。開始点からの距離では**ない** — 円は始点の近くへ
  戻るので、距離で見ると完成した円がクリックになる
- **直線かどうかで意図を判別しない**（文字列を横になぞるのは「この行」を指す正当な操作。
  Web版 solo-mode.md §1）。真横のなぞりはバウンズの高さが0になりGatewayが弾くので、
  縮退した軸だけを中心から`0.004`へ開く（`normalizedRegion`）
- 質問は**mouseUpで**発行する（mouseDownで撃つと囲みの始点に1ユニット払う）。印はmouseDownで
  即座に出るので、無反応には見えない
- 囲みでは**AX候補を引かない**。AXは要素を測るもので、囲みは「1つの要素でないもの」を囲む操作
- バブルは囲みの**外側**（`origin(besideFrame:)`）。中心基準だと囲んだ中身を自分で隠す
- 記録は`vision.region`（`drawn`=描いた点数／`kept`=capture内に残った点数／`candidates`）

実機で問題なく動いた（2026-08-24）。同じ実機確認で「押した瞬間にリングが出て、ドラッグすると線に
変わる」のが指摘され、**印はリリース時に出す**よう変えた（押した時点では、まだどちらのジェスチャーか
決まっていない）。

---

## 印は1つの型・1つの描画になった（2026-08-25）

**リング（丸）と枠（四角）は同じ`WashView.MarkShape`で、同じ`renderMark()`が描く。** それまでは
別物だった — リングは手描き（線3pt＋中央に十字）、枠はレイヤー（線2.5pt＋脈打つ減衰リング）。
同じ「これ」を意味する2つの印が似ていないと、ユーザーには別機能に見える。

- 線幅は**2.5pt**（暗いハローは6pt）で共通。**リングも脈打ち、減衰する線が周りに出る**
- **中央の十字は削除した。** クリックはクロスヘアではなく場所であり、十字は持っていない精度を主張する
- 形はリングのまま。ビートは`path(spread:)`で**外へ14pt広がる**方式（拡大ではない —
  800ptのツールバーを2倍にすると画面の半分にリングが飛ぶ）
- **焼き込み（`VisionPointerMark`）のマゼンタのリング＋十字は変えていない。** あれは観客がモデルで、
  実インターフェースに存在しない色と形であることが目的。画面上の印と同じにしてはいけない
- 死んでいた`draw(frame:)`（呼び出し元ゼロ）を削除した
- **軌跡（囲みで描く線）も同じ線幅にした**。同じ紫が3ptと2.5ptで出ていた理由が無かった

**色・線幅・角丸・ビートの正本は`MarkStyle`**（`BombSquad/Views/MarkStyle.swift`、2026-08-25）。
**Copilotが実画面へ出す枠も同じ印になった** — `systemRed`・角丸10・線4・独自グローだったものを
`MarkStyle.layer(for: .frame(...))`へ置き換え、**ビートも付いた**（案内は「探して、マウスを持って
いく」待ち時間があるので、静止した印が最も持たない面）。ウインドウの余白は`MarkStyle.outerReach`
（要素からの外側4＋ビート14＋下敷き6＝24）。**要素サイズで窓を作るとビートが切れる**。
**幕の色`WashStyle.tint`だけは分けて持つ** — 現在は印と同じRGBでアルファ違いだが、その一致は偶然で、
片方から導出すると幕の調整が製品中の印を動かす。

**呼び名の正本は [README](README.md) の「画面に出るものの呼び名」。** 幕／格子／スポットライト／
印（リング・枠・下敷き・ビート）／軌跡／バブル／焼き込み。**`halo`という語は使わない** —
黒い縁取りとビートの両方を指していて、次の編集で同じ線幅を与えてしまうため`backing`へ改名した。

**格子（幕の上の点）を濃くした**（2026-08-25）。2pt径・22pt間隔（面積の0.65%）は「見えない」ではなく
**見つけられない**濃さだった。4pt径・25pt間隔（2.0%＝インク3倍、点の数は8割）へ。
**白のアルファ0.16は上限ではない**（1.0まである）。実機でまだ薄ければ回すのはこの数字で、
まだ大きく余地がある（`latticeAlpha`）。

---

## 覆いの上での文字選択は撤退した（2026-08-24、再挑戦するなら必読）

**作って実機で試し、撤退した**（`9f9418e`と`4165932`をrevert）。「できない」ではなく
**まだらにしかできない**ことが理由である。

### 実機で見えた壁

- **Blinkはスタイルの変わり目でテキストをrun（塊）に分断する。** 文の途中で文字種が変わると
  そこから先が選択できない。run結合は書けるが、読み順と介在ノードの扱いがアプリごとに違い、
  際限がない
- **VS Codeのエディタ本体（Monaco）はAXへ文字を公開していない。** Iビームすら出ない。APIの側に
  文字が存在しないので、こちら側では直せない（同じウインドウのClaude Code出力側は選択できた）
- 結果、**同じ操作が「選択できる／一部だけ／全く効かない」の3通りに分かれる**。予測できない道具は
  信頼を失う。この製品が最優先してきた堅牢さと逆行する

### 次に誰かが挑むなら

- **`AXRangeForPosition`はBlinkではスタブで、どの座標にも0を返す**（`AXNumberOfCharacters`も0）。
  最初のprobeはこれを「成功」と読み違えた — 0はどのrunでも妥当なindexに見え、runごとに先頭文字が
  違うので位置に依存しているように見えた。**probeは「答えが返るか」ではなく「答えが入力に応じて
  変わるか」を確かめること**（app-iosの教訓の裏面）
- 代わりに**`AXBoundsForRange`の二分探索**でキャレットを解決する経路は成立した（文字矩形は正確・
  単調・サブms。実画面Chromeで「組織によって管理されています」の20%→70%＝「よって管理され」を
  確認、両端0.4ms）。**run内では動く。** 壁はrunの外にある
- 実装は`git show 4165932`に全部残っている（`VisionTextRangeReader`、覆いのIビーム／反転描画、
  `select()`ターン）。**Gateway側の語彙`ax_range_at_pointer`はrevertしていない** — 追加のみで
  害がなく、再挑戦の受け皿として残す価値があるため（`ec072ad`）

### 撤退しても要望は失われていない

確実な経路が2本ある。**①アプリで普通に選択→右Shift 2回**（選択できるアプリなら全文が正確に対象に
なる。既存のFocused Vision）。**②覆いの上で囲む→モデルが画像から文字を読む**（どこでも効き、
使い方を知らないユーザーの経路はこちら）。

**副産物として1件だけ残した修正**: `beginPointing()`が起動時の選択も捨てる。選択はプロンプトで
幾何より強いので、残っていると**以後のタップ全部が古い選択について答え続ける**。この不具合は
今回の機能より前から存在し、機能を撤退させても残る（`VisionSubjectExclusivityTests`で固定）。

---

## バブルの表示品質（対応済み・実機確認済み）

**2026-09-02: 配置の根本原因を「1ターン1配置が状態として存在しないこと」と確定し、短命ブランチ
`fix/vision-bubble-placement-state`で状態機械を実装した。** AX実測・囲みは送信前に配置確定、AXヒット
無しだけは最終検証済み枠まで本文を非表示で保持し、枠を反映してから全文を出す。ローカル幾何はモデル
推定からカードを守る。印は当初「必ず回答枠へ更新する」としたが、**2026-09-03に改訂**: 囲み・AX実測ありでは
回答枠を採用しない／実測要素と重なる時だけ採用する（マスタープランR14「印の根拠」。回答枠は場所を
確定できなかったクリックだけの妥協策で、`targetCandidateId`はラベル合わせなので文章と別の根拠になる）。
`vision.point`も`collectorEmpty / pointMiss / measured`へ分類し、全188テストが成功。オーナーの実機確認で、
(1) measuredは従来どおり早く流れる、(2) pointMiss/collectorEmptyは枠→全文の順、
(3) 囲み・measuredでも回答枠が出る、(4) 回答枠が別位置でも配置済みカードは動かない、まで成功した。
**この配置修正に残タスクはない。** 復帰点は
`pre-vision-bubble-placement-20260902`（`7804ad1`）。Gatewayと`0.2.2`は未変更。

**バブルは1つしかない。** 入場直後に右下へ出るものとクリック後に隣へ出るものは同じ
`NSHostingView`で、`present(on:bubble:)`がセッション開始時に1回だけ作り、以後は`placeBubble()`が
位置だけ動かす。見た目が違うのは中身の出入り（質問チップ・スクローラ・「案内を開始」ボタン・
入力欄の高さ）による。

8件直した（2026-08-24〜25）。実機で見てほしいのは、**答えの面積が実際に足りているか**である。

1. 高さを**行の整数倍**へ（`VisionBubbleView.answerHeight(within:)`）。文字の途中で切れない
2. 面積は**「画面に残っている分だけ」**（`VisionBubbleView.answerHeightBudget`）。半分では足りず、
   2026-08-25に**画面高の3分の2を上限とし、それが収まらない画面では「残り」を渡す**方式へ変えた。
   バブル以外の部分（ハンドル・質問チップ・入力欄・余白）の合計を`chromeHeight = 340`として明示し、
   「答え＋340が可視画面高を超えない」ことをテストで固定している。可視1117ptで36行→48行になった。
   **上限が画面そのものになったので、これ以上増やす余地はほぼ無い**（3倍＝108行は物理的に入らない）
3. **見えている面積を決めていたのは上限ではなく下限だった。** 上限を半分→「残り」に上げても
   ユーザーの見た目は**一切変わらなかった** — 箱が内容に密着するので、普通の長さの答えは上限に
   届かず、届いていたのは「2行半」だった。`answerMinHeight(within:)`＝**10行**を下限に入れて解決
   （`VisionBubbleLayoutTests`が実バブルをNSHostingViewで測って固定）。実測: 1行の答えで
   バブル総高131pt→**269pt**、長文で627pt→**850pt**（上限744.7ptに到達）。
   **上限だけを触った変更は「変わっていない」と報告される。見えている面積は下限が決める**
4. **入力欄が打った内容に追従する**（2026-08-25）。それまで`minHeight: 34 / maxHeight: 72`は
   **常に34ptに解決していた** — `NSViewRepresentable`は固有サイズを持たないので、intrinsic測定では
   minが勝ち、maxHeightは一度も効いていなかった（probeで実測）。`SendableTextEditor`が
   `usedRect + inset*2`を報告し、バブルが1行〜10行（上限は答えの予算の3分の1）で追従する
5. **送信ボタンを外した**（2026-08-25）。Enter送信・Shift+Enter改行は`SendableTextEditor`が
   最初から持っていたので、ボタンはキーボードが既にやることの2つ目の経路で、製品で最も狭い面から
   幅を取っていた。ヘルプ文言「Enterで送信、Shift+Enterで改行」が代わりを務める。
   **`PanelSendButton`はComposeでは現役**（3箇所）
6. 読む面と打つ面は**濃さだけで分ける**（2026-08-25）。`controlBackgroundColor`／`textBackgroundColor`
   ＋各面の1pt枠は、カードの上に2枚のカードが乗っているように見えた。**影はバブル自体の1つだけ**にし、
   面は`Color.primary.opacity(0.05)`（読む）と`0.10`（打つ）のフラットな地に。
   `NSScrollView.borderType = .noBorder`も明示（呼び出し側が縁を描くので二重になる）
7. **閉じるボタンを「解説を閉じる」に**（2026-08-25）。画面全体を覆うモードの唯一の出口が隅の×では、
   消したら何が起きるのか（解説か、幕か、アプリか）を推測させる。「終了」はアプリ終了に読めるので避けた
8. 待っている間の文言が**クリック後も「画面を読んでいます…」のまま**だった。`hasPointed`が
   present時の`session.pointer != nil`で固定され、rootViewは更新されないため。`beginPointing()`が
   立てる`VisionSession.isPointing`から引くようにした（`pointer`では駄目 — 古い場所を語らないために
   `beginPointing()`が意図的にnilへ落としており、文言が出ている待ち時間の全部でnilである）

---

## 音声入力は入力欄の中のマイクになった（2026-08-25）

**それまで音声入力にはボタンが1つも無かった。** 唯一の合図は、録音が始まった後にフォームの外へ出る
赤い`mic.fill`で、**使った後にしか現れないアフォーダンス**だった。ショートカット（右Shift長押し）を
知らないユーザーには到達手段が無い。

- `DictationButton`（`FoundationSharedViews`）を**入力欄の面の中**へ。Composeの下書き欄と
  Visionバブルの両方。灰色のマイク → 録音中は赤い`mic.fill` → 文字起こし中はスピナー
- **クリックで開始、もう一度クリックで停止。** キーは従来どおりhold-to-talk。
  離すものが無いボタンにhold-to-talkは作れないので、2つの操作モデルは妥協ではなく定石
- 入口は`SessionCoordinator.toggleDictation()`1つで、開始・停止・文字起こし・後始末は
  キーの経路と完全に同じコードを通る。ビューへは`session.onToggleDictation`で渡す
- **Compose外側の「録音中…」「文字起こし中…」は削除した**（同じことを2箇所で言うと、押した
  コントロールではない場所を見る癖がつく）
- 赤は**状態**なので`MarkStyle`の紫は使わない
- **カーソルもヒントも明示が要る**（上記の表を参照）。`.help()`は覆いの上では見えないので、
  `.hoverHint()`（自分の窓に450ms遅延で描く）へ置き換えた。Composeでも同じ仕組みを使う —
  1つの機能に2つの提示方法を持たない

---

## 未確認のまま残っていること

バブル配置と印の更新は2026-09-02に実機確認済みのため、この一覧から除外した。以下は別件として残る。

- **バブルのすぐ外側のクリックが下のアプリに届くか。** 影をSwiftUIからAppKitの窓影へ移し、
  窓をカードと同じ大きさにした（`hasShadow`＋`invalidateShadow`）。**影の見た目も一緒に見る** —
  描き手が変わったので、丸角に沿っているか、濃さが変わっていないか
- **案内中、バブルのすぐ外側の帯**（同上）

**2026-08-26に入れた演出も実機未確認**（`WashStyle`に集約）。**3つ目の「重力」（格子がカーソルへ寄る歪み）は
2026-09-03に撤去した** — 実機で安っぽく見えたため。格子は動かない:

- **走査線**: 右Shift 2回と案内の撮り直しで、幕が**下から上へ光の前縁とともに現れる**か
  （0.42秒・強い加速`(0.7,0,0.84,0)`・前縁の光120pt・白22%。数字は`WashStyle.sweep*`）。「幕の上を帯が通る」初版は
  実機で不自然と判定され、幕自体を露出させる形へ変えた（2026-08-26）
- **撮り直しのキュー**が黒い暗転ではなく**紫の幕**になっているか（撮影範囲だけ）。目のアイコンと枠は撤去
- **格子の負荷**: ドット個別描画のまま（1728×1117・36pt間隔で約1,500ドット）。`fillEllipse`を1個ずつ
  呼ぶのは意図的（1本のパスにまとめると3倍遅い、実測済み）。`WashSheetTests`が1枚16ms以内を固定する

案内で一度も通していない経路:

- **「再確認」ボタン**
- **違う物を押した時**に「今の画面から」の次の一手が返るか（「戻ってください」と言わないこと。
  Gatewayのguidance分岐が固定しているはずの挙動）
- **画面が変わらなかった時**の注記「操作は検知しましたが、画面に変化は見えませんでした」
  （`copilotSawNoChange`）。R17でスレッドの行になり、修飾する一手の直前に出る
- **×で解説へ戻った後、別の場所をクリックするとバブルが印の隣へ戻るか**（ドラッグした位置が
  リセットされること）

見た目の調整（数字は分かっている。実画面で見ないと決められない）:

- **スレッドの面積が足りているか。** 下限10行（`answerMinHeight`）はスレッド全体の下限として
  効いている（1つの回答を包む読む面はR17で無くなった）
- **格子の濃さ。** `latticeAlpha`は現在0.16で**上限は1.0**。まだ大きく余地がある
- **スポットライトの`reach = 672pt`** はWeb版の値のままで、実画面での見え方は未調整

環境:

- **ネイティブメニューが開いた画面で覆いを出すとメニューは閉じるか。** `capture.display`は
  2026-08-25時点で105件すべて`menu=false`で、**普段の利用ではこの状況自体が起きていない**。
  R12の「開いたプルダウンを説明する」に着手する時まで保留でよい
- 覆いがフルスクリーンアプリ・Mission Control・Stage Managerでどう振る舞うか
- マルチディスプレイで、覆っていない画面のクリックがどうなるか

---

## 別セッションで扱うと決めたもの

- **未ログイン初回起動の第一印象。** 現在は「画面の読み取りを開始できませんでした。」だけが出る。
  起動の合図・初回の挨拶・その場でのサインインを設計する。正本はマスタープランの
  「次期改善候補 › 未ログイン初回起動の第一印象」

---

## 手を出す前に知っておくべきこと

| 事実 | なぜ重要か |
|---|---|
| **キーチェーンのプロンプトは原因が分かって直した** | 診断手順は [AGENTS.md](AGENTS.md)。**新規ユーザーは1回も聞かれない**（作成者はACLに載るので無音。実測済み） |
| **CLI検証ビルド（`CODE_SIGNING_ALLOWED=NO`）の`.app`を起動しない** | 署名が無くTCC許可もキーチェーンACLも成立しない。実機確認はXcodeから |
| **CLIビルドは必ず`-derivedDataPath`で隔離する** | 既定のDerivedDataを汚すとTCCが毎ビルド死ぬ |
| **座標の変換は`VisionPointerResolver`の2関数だけ** | 3つ目の式を書くと二次ディスプレイでずれる。12件のテストで固定してある |
| **Gatewayは候補の矩形をモデルへ渡さない** | 渡すのは`hit_candidate_id`が指すidとrole/labelだけ。矩形はバブルの置き場所と枠に使う実測値で、モデルの根拠は焼き込んだマークである |
| **AX候補は操作系13ロールだけ** | 本文・画像・グラフ・canvasはクリック点でAX候補に当たらないことがある。ただし候補一覧そのものが0件なら収集失敗も疑い、`collectorEmpty / pointMiss / measured`で切り分ける |
| **印は常に1つだけ**（実装済み） | リングはリリース時に出る。AX枠が出たら消える。**回答枠が印になるのはAXヒット無しのクリックだけ**（2026-09-03）。囲みでは軌跡が、実測ありではAX枠が残る。`highlight=gestureKept`が棄却の記録 |
| **指し直しは新しい主題**（`turns`を捨てる） | Web版は捨てなかった版でタップのたびに最初の回答が返った |
| **覆いのパネルではシステムのツールチップが見えない** | AppKitはツールチップを**別ウインドウ**のポップアップ階層に描く。覆いは`.screenSaver`階層で全画面を覆うので、ツールチップは正しく描かれた上で必ず裏に回る。**z順の修正では直せない**（公開APIで階層を上げられない）。ヒントは`.hoverHint()`で自分の窓に描く |
| **覆いのパネルではカーソル矩形が動かない** | keyにはなるがmainにはならないため。テキスト欄がIビームにした後、誰も戻さないので**バブル中の全ボタンがIビームのまま**になる。押せるものには`.arrowCursorOnHover()`を付ける（`push`ではなく`set` — popを払う相手が消えると全アプリがカーソルを握ったままになる） |
| **`.nonactivatingPanel`＋`makeKey()`は同期的にキーを取る** | `NSApp.activate`は非同期で、出した直後はまだキーでない（実測） |
| **覆いに描いた印は撮影に写らない** | 自アプリ除外はアプリ単位。だから焼き込みは選択肢ではなく必然（送信バイトを開いて実測済み） |
| **`api-gateway`はmainへのpushが本番デプロイ** | プロンプトを触ると公開中のmacOSクライアントの挙動も変わる |
| **パネルはComposeだけ**（F11・R15） | Visionも案内も覆い＋バブル。ストリップは無い。処理情報の情報ボタン・「案内を開始」・状態行もバブル |
| **バブルは覆いの子パネル**（R15） | 覆いが`ignoresMouseEvents=true`でも受ける。IME確定・キーの受け渡しはG1で測定済み |
| **窓の透明な余白はクリックを飲む。`hitTest`でnilを返しても下へは渡らない** | 窓は受け取ってから捨てるだけ。影は必ずAppKit（`hasShadow`）に描かせ、**窓はカードと同じ大きさにする** |
| **`clearAnswerFrame`は回答が出した枠だけを下ろす** | ユーザーのジェスチャーが出したリングは持ち物ではない。混ぜるとクリックの瞬間にリングが消える（G8の回帰） |
| **案内中のEscはキーボードのフォーカスを持っている方へ行く** | 案内へ入ってもバブルが持ったままなので、普通はEscでUniversal I/Oが閉じる。**案内された対象をクリックするとフォーカスが対象アプリへ移り、以後のEscはそちらのもの**。どの状況でも効く出口は右Shift 2回と× |
| **案内中のグローバル監視にはバブルへのクリックも届く** | 「自アプリのイベントは来ない」は誤り（実機で確認）。対象アプリが前面でこちらが非活性だと届く。**バブルの矩形の中のクリックは操作に数えない**（`advancesGuidance`） |
| **gitは必ず`git -C <絶対パス>`** | シェルは毎回app-macへ戻る。過去に2回、別リポジトリのコミットが混入した |

---

## 検証

```bash
xcodegen generate
xcodebuild -project BombSquad.xcodeproj -scheme BombSquad -configuration Debug \
  -derivedDataPath /tmp/universal-io-derived -destination 'platform=macOS' \
  CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO test
```

実機は**Xcodeから Run**する。動作記録は「管理画面 › 設定 › 動作記録」。R14で足した記録は
`capture.display`（ms・画素数・除外数・メニューの有無・**ディスプレイID**）、
`vision.point`（候補数・AXヒットの有無）、`vision.region`（描いた点数・capture内に残った点数・候補数）、
`vision.mark`（マークが焼けたか・送信画像の寸法・指した位置の千分率）、
`auth.keychain`（restored / absent / refused）。

**送信画像そのものを見る**（DEBUGビルドのみ、pointingの回だけ）:

```bash
ls -t /tmp/universal-io-wire-dumps/ | head
```

ファイル名に指した位置の千分率と焼き込みの成否が入っている。座標が疑わしくなったら、
仮説を立てる前にこれを開くこと。
